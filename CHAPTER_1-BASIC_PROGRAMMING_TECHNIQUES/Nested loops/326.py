"""
ГЛАВА 1
ОСНОВНЫЕ ПРИЁМЫ ПРОГРАММИРОВАНИЯ
10. Вложенные циклы

326. Найти наименьшее натуральное число n, представимое двумя различными способами в виде суммы кубов двух натуральных чисел x ^ 3 + y ^ 3 * (x >= y).
"""


def main():
    max_x = 100  # Достаточный предел, так как искомое число 1729 требует x ≤ 12
    sums = {}    # Словарь: сумма -> (x, y) для первого представления

    for x in range(1, max_x + 1):
        for y in range(1, x + 1):  # гарантируем x ≥ y
            s = x**3 + y**3
            if s in sums:
                # Найдено второе представление
                prev_x, prev_y = sums[s]
                print(f"Наименьшее число n = {s}")
                print(f"Представления: {prev_x}³ + {prev_y}³ = {prev_x**3} + {prev_y**3}")
                print(f"и {x}³ + {y}³ = {x**3} + {y**3}")
                return
            else:
                sums[s] = (x, y)

    print("Число не найдено в заданном диапазоне. Увеличьте max_x.")

if __name__ == "__main__":
    main()
    input("\nНажмите Enter, чтобы завершить программу.")
