"""
ГЛАВА 1
ОСНОВНЫЕ ПРИЁМЫ ПРОГРАММИРОВАНИЯ
14. Вычисления с хранением последовательностей, число членов
которых зависит от исходных данных *)
*) В некоторых языках программирования допускаются массивы с динамическими границами, и это снимает многие трудности в решении 
задач; в этом случае настоящий параграф продолжает § 9. В Паскале же, например, где такие массивы не допускаются, естественно 
использовать списки. Возможный вид этих списков указан в задачах 531-534. Для работы со списками полезны процедуры вставки 
элемента в начало списка, вставки элемента в конец списка, удаление 
элемента и т. д. (эти процедуры отдельно рассмотрены в §36). Для решения задач этого параграфа можно использовать и файлы, но это
резко увеличивает время выполнения программы и имеет смысл в том 
случае, когда все исходные данные не помещаются в памяти вычислительной машины.

552. Даны символы s1, s2, ... Известно, что символ si отличен от точки и что среди s2, s3, .... имеется хотя бы одна точка. Пусть s1, ...., sn - символы, предшествующие первой точке (n заранее неизвестно).
Получить:
а) последовательность sn, sn-1, ..., s1;
б) последовательность s1, s3, ...., sn, если n - нечетное, и последовательность s2, s4, ..., sn, если n - четное.
"""


import random
import string


def get_sequence():
    """Выбор способа ввода последовательности символов."""
    print("Задача 552: Обработка последовательности до первой точки")
    print("Выберите способ ввода:")
    print("1 — Ручной ввод")
    print("2 — Случайная генерация")
    print("3 — Готовые примеры")

    while True:
        choice = input("Ваш выбор (1/2/3): ").strip()
        if choice in ('1', '2', '3'):
            break
        print("Ошибка: выберите 1, 2 или 3.")

    if choice == '1':
        while True:
            print("Введите последовательность символов (первый символ не точка, среди остальных есть точка):")
            raw = input().strip()
            if not raw:
                print("Пустая строка недопустима.")
                continue
            if raw[0] == '.':
                print("Первый символ не должен быть точкой.")
                continue
            if '.' not in raw[1:]:
                print("Среди символов после первого должна быть хотя бы одна точка.")
                continue
            return raw

    elif choice == '2':
        # Генерируем случайную последовательность, удовлетворяющую условиям
        # Первый символ – не точка, остальные могут быть любыми, но обязательно есть точка.
        first_char = random.choice(string.ascii_letters + string.digits)
        # Длина от 3 до 15 (чтобы после первого символа было место для точки)
        length = random.randint(3, 15)
        rest = [random.choice(string.ascii_letters + string.digits) for _ in range(length - 1)]
        # Вставляем точку в случайное место среди остальных (индекс от 1 до length-1)
        dot_pos = random.randint(1, length - 1)
        rest.insert(dot_pos - 1, '.')  # потому что индексация с 0, а dot_pos от 1
        sequence = first_char + ''.join(rest)
        print(f"\nСгенерирована случайная последовательность: {repr(sequence)}")
        return sequence

    else:  # готовые примеры
        examples = [
            "abc.def",
            "xy.z",
            "hello.world",
            "123.456",
            "a.bcdef",
        ]
        print("\nГотовые примеры:")
        for idx, seq in enumerate(examples, 1):
            print(f"{idx}: {repr(seq)}")
        while True:
            try:
                num = int(input("Выберите номер примера: "))
                if 1 <= num <= len(examples):
                    return examples[num - 1]
                else:
                    print(f"Номер должен быть от 1 до {len(examples)}.")
            except ValueError:
                print("Ошибка ввода. Введите целое число.")


def main():
    sequence = get_sequence()

    # Находим индекс первой точки
    dot_index = sequence.find('.')
    if dot_index == -1:
        print("Ошибка: точка не найдена.")
        return
    if dot_index == 0:
        print("Ошибка: первый символ — точка.")
        return

    # Последовательность s1...sn (до первой точки)
    s = sequence[:dot_index]
    n = len(s)

    print("\nИсходная строка:", repr(sequence))
    print("Последовательность до первой точки s:", repr(s))
    print(f"n = {n}")

    # а) Обратный порядок
    result_a = s[::-1]
    print("\nа) Обратная последовательность sn...s1:")
    print("  ", result_a)

    # б) Выбор каждого второго символа
    if n % 2 != 0:
        # n нечётное: берём s1, s3, s5, ...
        result_b = s[0::2]
        print(f"\nб) n нечётное, берём s1,s3,...,sn:")
    else:
        # n чётное: берём s2, s4, s6, ...
        result_b = s[1::2]
        print(f"\nб) n чётное, берём s2,s4,...,sn:")
    print("  ", result_b)


if __name__ == "__main__":
    main()
    input("\nНажмите Enter, чтобы завершить программу.")