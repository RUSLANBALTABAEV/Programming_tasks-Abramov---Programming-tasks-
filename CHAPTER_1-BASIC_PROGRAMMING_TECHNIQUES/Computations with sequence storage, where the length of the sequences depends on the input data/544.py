"""
ГЛАВА 1
ОСНОВНЫЕ ПРИЁМЫ ПРОГРАММИРОВАНИЯ
14. Вычисления с хранением последовательностей, число членов
которых зависит от исходных данных *)
*) В некоторых языках программирования допускаются массивы с динамическими границами, и это снимает многие трудности в решении 
задач; в этом случае настоящий параграф продолжает § 9. В Паскале же, например, где такие массивы не допускаются, естественно 
использовать списки. Возможный вид этих списков указан в задачах 531-534. Для работы со списками полезны процедуры вставки 
элемента в начало списка, вставки элемента в конец списка, удаление 
элемента и т. д. (эти процедуры отдельно рассмотрены в §36). Для решения задач этого параграфа можно использовать и файлы, но это
резко увеличивает время выполнения программы и имеет смысл в том 
случае, когда все исходные данные не помещаются в памяти вычислительной машины.

544. Даны натуральное число n, действительные числа a1, ..., an. 
Если в последовательность a1, ..., an есть хотя бы один член, меньший, чем - 3, то все неотрицательные члены заменить их квадратами, оставив остальные члены без изменения; в противном случае домножить все члены на 0.1. 
"""


import random


def get_data():
    """Выбор способа ввода последовательности."""
    print("Задача 544: Преобразование последовательности по условию")
    print("Выберите способ ввода:")
    print("1 — Ручной ввод")
    print("2 — Случайная генерация")
    print("3 — Готовые примеры")

    while True:
        choice = input("Ваш выбор (1/2/3): ").strip()
        if choice in ('1', '2', '3'):
            break
        print("Ошибка: выберите 1, 2 или 3.")

    if choice == '1':
        while True:
            try:
                n = int(input("Введите количество чисел n (>=1): "))
                if n <= 0:
                    print("n должно быть положительным.")
                    continue
                print(f"Введите {n} чисел через пробел:")
                a = list(map(float, input().split()))
                if len(a) != n:
                    print(f"Ожидалось {n} чисел, получено {len(a)}.")
                    continue
                return n, a
            except ValueError:
                print("Ошибка ввода. Повторите.")

    elif choice == '2':
        n = random.randint(3, 6)
        a = [round(random.uniform(-10, 10), 2) for _ in range(n)]
        print(f"\nСгенерированы случайные данные: n = {n}")
        print("Последовательность a:", a)
        return n, a

    else:  # готовые примеры
        examples = [
            (4, [-4.0, 2.0, 0.0, 5.0]),          # есть < -3
            (3, [-2.0, 4.0, 6.0]),               # нет < -3
            (5, [-3.5, 0.5, 2.0, 3.0, 4.0]),     # есть < -3
            (6, [-10.0, -5.0, 0.5, 2.0, 3.0, 4.0]), # есть < -3
        ]
        print("\nГотовые примеры:")
        for idx, (n_val, a_vals) in enumerate(examples, 1):
            print(f"{idx}: n={n_val}, a={a_vals}")
        while True:
            try:
                num = int(input("Выберите номер примера: "))
                if 1 <= num <= len(examples):
                    return examples[num - 1]
                else:
                    print(f"Номер должен быть от 1 до {len(examples)}.")
            except ValueError:
                print("Ошибка ввода. Введите целое число.")


def main():
    n, a = get_data()

    # Проверяем условие: есть ли хотя бы один член, меньший, чем -3
    has_less_than_minus_3 = any(x < -3.0 for x in a)

    # Выполняем преобразование
    if has_less_than_minus_3:
        print("\nУсловие выполнено: есть член < -3.")
        print("Действие: все неотрицательные члены заменить их квадратами.")
        result = [x**2 if x >= 0.0 else x for x in a]
    else:
        print("\nУсловие НЕ выполнено: членов < -3 не найдено.")
        print("Действие: домножить все члены на 0.1.")
        result = [x * 0.1 for x in a]

    # Вывод результатов
    print("\nРезультаты:")
    print(f"Исходные данные: n = {n}")
    print(f"Последовательность a: {a}")
    print(f"Преобразованная последовательность: {result}")


if __name__ == "__main__":
    main()
    input("\nНажмите Enter, чтобы завершить программу.")