"""
ГЛАВА 1
ОСНОВНЫЕ ПРИЁМЫ ПРОГРАММИРОВАНИЯ
14. Вычисления с хранением последовательностей, число членов
которых зависит от исходных данных *)
*) В некоторых языках программирования допускаются массивы с динамическими границами, и это снимает многие трудности в решении 
задач; в этом случае настоящий параграф продолжает § 9. В Паскале же, например, где такие массивы не допускаются, естественно 
использовать списки. Возможный вид этих списков указан в задачах 531-534. Для работы со списками полезны процедуры вставки 
элемента в начало списка, вставки элемента в конец списка, удаление 
элемента и т. д. (эти процедуры отдельно рассмотрены в §36). Для решения задач этого параграфа можно использовать и файлы, но это
резко увеличивает время выполнения программы и имеет смысл в том 
случае, когда все исходные данные не помещаются в памяти вычислительной машины.

551. Даны натуральное число n, символы s1, ...., sn. Будем рассматривать слова, образованные входящими в последовательность s1, ...., sn символами (см. задачу 269). Ниже описываются преобразования, каждое из которых следует произвести три выполнении указанного условия. Затем последовательность вне зависимости от того, подвергалась она или нет преобразованию, должна быть отредактирована следующим образом. Должны быть удалены группы пробелов, которыми начинается и заканчивается последовательность, а каждая внутренняя группа пробелов должна быть заменена одним пробелом.
Преобразования:
а) если общее кол-во слов больше единицы и нечетно, то удалить первое слово;
б) если последнее слово начинается буквой a и общее число слов больше единицы, то переставить последнее слово в начало последовательности, отделив его пробелом от s1;
в) если первое и последние слова совпадают и общее число слов больше единицы, то удалить первое и последнее слова, а оставшиеся символы переставить в обратном порядке.
"""


import random
import string


def get_sequence():
    """Выбор способа ввода последовательности символов."""
    print("Задача 551: Преобразование слов в последовательности")
    print("Выберите способ ввода:")
    print("1 — Ручной ввод")
    print("2 — Случайная генерация")
    print("3 — Готовые примеры")

    while True:
        choice = input("Ваш выбор (1/2/3): ").strip()
        if choice in ('1', '2', '3'):
            break
        print("Ошибка: выберите 1, 2 или 3.")

    if choice == '1':
        while True:
            try:
                n = int(input("Введите количество символов n: "))
                if n <= 0:
                    print("n должно быть положительным.")
                    continue
                print("Введите последовательность символов (включая пробелы).")
                print("Пробелы считаются символами.")
                raw = input()
                if len(raw) != n:
                    print(f"Ожидалось {n} символов, введено {len(raw)}.")
                    continue
                return n, raw
            except ValueError:
                print("Ошибка ввода. Повторите.")

    elif choice == '2':
        n = random.randint(10, 30)
        # Генерируем случайную строку из букв, цифр и пробелов.
        # Вероятность пробела – 20%, чтобы слова встречались, но не слишком часто.
        alphabet = string.ascii_letters + string.digits + ' ' * 2
        s = ''.join(random.choice(alphabet) for _ in range(n))
        print(f"\nСгенерирована случайная последовательность длиной {n}:")
        print("Строка:", repr(s))
        return n, s

    else:  # готовые примеры
        examples = [
            (15, "abc ab bc a"),        # слова: abc, ab, bc, a
            (12, "hello a world"),      # слова: hello, a, world
            (9, "test a test"),         # слова: test, a, test
            (20, "  multiple   spaces  "), # множественные пробелы
        ]
        print("\nГотовые примеры:")
        for idx, (n_val, seq) in enumerate(examples, 1):
            print(f"{idx}: n={n_val}, последовательность: {repr(seq)}")
        while True:
            try:
                num = int(input("Выберите номер примера: "))
                if 1 <= num <= len(examples):
                    return examples[num - 1]
                else:
                    print(f"Номер должен быть от 1 до {len(examples)}.")
            except ValueError:
                print("Ошибка ввода. Введите целое число.")


def main():
    n, sequence = get_sequence()
    print("\nИсходная последовательность:", repr(sequence))

    # Разбиваем на слова (пробелы – разделители, любые группы пробелов игнорируются)
    words = sequence.split()
    print("Слова:", words)
    print(f"Количество слов: {len(words)}")

    # Преобразование а)
    if len(words) > 1 and len(words) % 2 == 1:
        print(f"а) Условие выполнено: слов >1 и нечётное количество. Удаляем первое слово '{words[0]}'.")
        words.pop(0)
    else:
        print("а) Условие не выполнено (слов не больше 1 или чётное количество).")

    # Преобразование б)
    if len(words) > 1 and words[-1].startswith('a'):
        print(f"б) Условие выполнено: последнее слово '{words[-1]}' начинается на 'a'. Переставляем его в начало.")
        last = words.pop()
        words.insert(0, last)
    else:
        print("б) Условие не выполнено (слов не больше 1 или последнее слово не на 'a').")

    # Преобразование в)
    if len(words) > 1 and words[0] == words[-1]:
        print(f"в) Условие выполнено: первое '{words[0]}' и последнее '{words[-1]}' совпадают. Удаляем их и переворачиваем оставшиеся.")
        words.pop()          # удаляем последнее
        words.pop(0)         # удаляем первое
        words.reverse()      # переворачиваем оставшиеся
    else:
        print("в) Условие не выполнено (слов не больше 1 или первое и последнее не совпадают).")

    print("\nСлова после преобразований:", words)

    # Финальное редактирование: удаляем пробелы по краям и заменяем внутренние группы пробелов одним пробелом.
    # Это эквивалентно соединению слов через один пробел.
    edited = ' '.join(words)

    print("Отредактированная последовательность:", repr(edited))


if __name__ == "__main__":
    main()
    input("\nНажмите Enter, чтобы завершить программу.")