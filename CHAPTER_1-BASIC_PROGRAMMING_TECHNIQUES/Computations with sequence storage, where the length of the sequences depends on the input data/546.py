"""
ГЛАВА 1
ОСНОВНЫЕ ПРИЁМЫ ПРОГРАММИРОВАНИЯ
14. Вычисления с хранением последовательностей, число членов
которых зависит от исходных данных *)
*) В некоторых языках программирования допускаются массивы с динамическими границами, и это снимает многие трудности в решении 
задач; в этом случае настоящий параграф продолжает § 9. В Паскале же, например, где такие массивы не допускаются, естественно 
использовать списки. Возможный вид этих списков указан в задачах 531-534. Для работы со списками полезны процедуры вставки 
элемента в начало списка, вставки элемента в конец списка, удаление 
элемента и т. д. (эти процедуры отдельно рассмотрены в §36). Для решения задач этого параграфа можно использовать и файлы, но это
резко увеличивает время выполнения программы и имеет смысл в том 
случае, когда все исходные данные не помещаются в памяти вычислительной машины.

546. Даны натуральные числа n, m, символы s1, ...., sn (m < n). 
Получить последовательность символов:
а) sm + 1, sm + 2, ..., sn, s1, ..., sm;
б) sm + 1, sm + 2, ..., sn, sm, ..., s1;
в) sn, sn-1, ..., sm + 1, s1, ..., sm.
"""


import random
import string


def get_data():
    """Выбор способа ввода последовательности."""
    print("Задача 546: Получение последовательностей символов по заданным правилам")
    print("Выберите способ ввода:")
    print("1 — Ручной ввод")
    print("2 — Случайная генерация")
    print("3 — Готовые примеры")

    while True:
        choice = input("Ваш выбор (1/2/3): ").strip()
        if choice in ('1', '2', '3'):
            break
        print("Ошибка: выберите 1, 2 или 3.")

    if choice == '1':
        while True:
            try:
                n = int(input("Введите количество символов n (>=1): "))
                if n <= 0:
                    print("n должно быть положительным.")
                    continue
                m = int(input(f"Введите число m (0 <= m < {n}): "))
                if m < 0 or m >= n:
                    print(f"m должно быть в диапазоне от 0 до {n-1}.")
                    continue
                print(f"Введите {n} символов через пробел (каждый символ — один знак):")
                s = input().split()
                if len(s) != n:
                    print(f"Ожидалось {n} символов, получено {len(s)}.")
                    continue
                if any(len(ch) != 1 for ch in s):
                    print("Каждый символ должен быть одним знаком (без пробелов).")
                    continue
                return n, m, s
            except ValueError:
                print("Ошибка ввода. Повторите.")

    elif choice == '2':
        n = random.randint(2, 10)          # минимум 2, чтобы m < n и был смысл
        m = random.randint(0, n - 1)
        # генерируем n случайных строчных букв
        s = [random.choice(string.ascii_lowercase) for _ in range(n)]
        print(f"\nСгенерированы случайные данные: n = {n}, m = {m}")
        print("Последовательность s:", s)
        return n, m, s

    else:  # готовые примеры
        examples = [
            (5, 2, ['a', 'b', 'c', 'd', 'e']),       # m в середине
            (4, 0, ['x', 'y', 'z', 'w']),            # m = 0
            (6, 5, ['1', '2', '3', '4', '5', '6']),  # m = n-1
            (7, 3, ['п', 'р', 'и', 'м', 'е', 'р', 'ы']), # кириллица
        ]
        print("\nГотовые примеры:")
        for idx, (n_val, m_val, s_vals) in enumerate(examples, 1):
            print(f"{idx}: n={n_val}, m={m_val}, s={s_vals}")
        while True:
            try:
                num = int(input("Выберите номер примера: "))
                if 1 <= num <= len(examples):
                    return examples[num - 1]
                else:
                    print(f"Номер должен быть от 1 до {len(examples)}.")
            except ValueError:
                print("Ошибка ввода. Введите целое число.")


def main():
    n, m, s = get_data()

    # Выполняем преобразования
    # а) sm+1, ..., sn, s1, ..., sm
    result_a = s[m:] + s[:m]

    # б) sm+1, ..., sn, sm, ..., s1
    result_b = s[m:] + s[:m][::-1]

    # в) sn, sn-1, ..., sm+1, s1, ..., sm
    result_c = s[n-1:m-1:-1] + s[:m] if m > 0 else s[::-1]

    # Вывод результатов
    print("\nРезультаты:")
    print(f"Исходные данные: n = {n}, m = {m}")
    print(f"Последовательность s: {s}")
    print()
    print("а) sm+1, ..., sn, s1, ..., sm:")
    print(f"   {result_a}")
    print()
    print("б) sm+1, ..., sn, sm, ..., s1:")
    print(f"   {result_b}")
    print()
    print("в) sn, sn-1, ..., sm+1, s1, ..., sm:")
    print(f"   {result_c}")


if __name__ == "__main__":
    main()
    input("\nНажмите Enter, чтобы завершить программу.")