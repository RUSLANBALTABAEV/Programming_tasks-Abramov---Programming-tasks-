"""
ГЛАВА 1
ОСНОВНЫЕ ПРИЁМЫ ПРОГРАММИРОВАНИЯ
14. Вычисления с хранением последовательностей, число членов
которых зависит от исходных данных *)
*) В некоторых языках программирования допускаются массивы с динамическими границами, и это снимает многие трудности в решении 
задач; в этом случае настоящий параграф продолжает § 9. В Паскале же, например, где такие массивы не допускаются, естественно 
использовать списки. Возможный вид этих списков указан в задачах 531-534. Для работы со списками полезны процедуры вставки 
элемента в начало списка, вставки элемента в конец списка, удаление 
элемента и т. д. (эти процедуры отдельно рассмотрены в §36). Для решения задач этого параграфа можно использовать и файлы, но это
резко увеличивает время выполнения программы и имеет смысл в том 
случае, когда все исходные данные не помещаются в памяти вычислительной машины.

540. Даны натуральное число n, целые числа a1, ..., a2n. 
Выяснить, верно ли, что для i = 1, 2, ..., n выполнено:
а) ai = -an+i;
б) ai = 2 * an - i + a2 * n - i + 1;
в) ai + a2 * n - i + 1 > 17;
г) a2 * n - i + 1 < ai <= a2 * n - i.
"""


def main():
    print("=== ЗАДАЧА 540: Проверка условий для элементов последовательности ===\n")
    
    try:
        n = int(input("Введите натуральное число n: "))
        if n < 1:
            print("Ошибка: n должно быть больше 0.")
            return
            
        total_nums = 2 * n
        print(f"Введите {total_nums} целых чисел через пробел:")
        a = list(map(int, input().split()))
        
        if len(a) != total_nums:
            print(f"Ошибка: введено {len(a)} чисел, а ожидается {total_nums}.")
            return

        # Инициализируем флаги. Предполагаем, что все условия верны.
        is_a = True
        is_b = True
        is_c = True
        is_d = True
        
        # Ищем индекс, на котором условие нарушается впервые (для вывода подробной информации)
        fail_a, fail_b, fail_c, fail_d = 0, 0, 0, 0

        # Перебираем i от 1 до n
        for i in range(1, n + 1):
            # Преобразование математических индексов (начиная с 1) в индексы Python (начиная с 0)
            idx_ai = i - 1
            idx_an_plus_i = n + i - 1
            idx_an_minus_i = n - i - 1
            idx_a2n_minus_i_plus_1 = 2 * n - i
            idx_a2n_minus_i = 2 * n - i - 1

            ai = a[idx_ai]
            an_plus_i = a[idx_an_plus_i]
            an_minus_i = a[idx_an_minus_i]
            a2n_minus_i_plus_1 = a[idx_a2n_minus_i_plus_1]
            a2n_minus_i = a[idx_a2n_minus_i]

            # а) ai = -a_{n+i}
            if is_a and ai != -an_plus_i:
                is_a = False
                fail_a = i

            # б) ai = 2*a_{n-i} + a_{2n-i+1}
            if is_b and ai != 2 * an_minus_i + a2n_minus_i_plus_1:
                is_b = False
                fail_b = i

            # в) ai + a_{2n-i+1} > 17
            if is_c and ai + a2n_minus_i_plus_1 <= 17:
                is_c = False
                fail_c = i

            # г) a_{2n-i+1} < ai <= a_{2n-i}
            if is_d and not (a2n_minus_i_plus_1 < ai <= a2n_minus_i):
                is_d = False
                fail_d = i

        # Вывод результатов
        print("\n--- РЕЗУЛЬТАТЫ ПРОВЕРКИ ---")
        if is_a:
            print("а) Выполняется для всех i (ai = -a_{n+i})")
        else:
            print(f"а) НЕ выполняется. Первое нарушение на i = {fail_a}")

        if is_b:
            print("б) Выполняется для всех i (ai = 2*a_{n-i} + a_{2n-i+1})")
        else:
            print(f"б) НЕ выполняется. Первое нарушение на i = {fail_b}")

        if is_c:
            print("в) Выполняется для всех i (ai + a_{2n-i+1} > 17)")
        else:
            print(f"в) НЕ выполняется. Первое нарушение на i = {fail_c}")

        if is_d:
            print("г) Выполняется для всех i (a_{2n-i+1} < ai <= a_{2n-i})")
        else:
            print(f"г) НЕ выполняется. Первое нарушение на i = {fail_d}")

    except ValueError:
        print("Ошибка: введите корректные целые числа.")

if __name__ == "__main__":
    main()
    input("\nНажмите Enter, чтобы завершить программу.")