"""
ГЛАВА 1
ОСНОВНЫЕ ПРИЁМЫ ПРОГРАММИРОВАНИЯ
14. Вычисления с хранением последовательностей, число членов
которых зависит от исходных данных *)
*) В некоторых языках программирования допускаются массивы с динамическими границами, и это снимает многие трудности в решении 
задач; в этом случае настоящий параграф продолжает § 9. В Паскале же, например, где такие массивы не допускаются, естественно 
использовать списки. Возможный вид этих списков указан в задачах 531-534. Для работы со списками полезны процедуры вставки 
элемента в начало списка, вставки элемента в конец списка, удаление 
элемента и т. д. (эти процедуры отдельно рассмотрены в §36). Для решения задач этого параграфа можно использовать и файлы, но это
резко увеличивает время выполнения программы и имеет смысл в том 
случае, когда все исходные данные не помещаются в памяти вычислительной машины.

549. Даны натуральное число n, символы s1, ..., sn. Получить последовательность символов, содержащую только последние вхождения каждого символа с сохранением взаимного порядка этих вхождений.
"""


import random
import string


def get_sequence():
    """Выбор способа ввода последовательности символов."""
    print("Задача 549: Последние вхождения каждого символа")
    print("Выберите способ ввода:")
    print("1 — Ручной ввод")
    print("2 — Случайная генерация")
    print("3 — Готовые примеры")

    while True:
        choice = input("Ваш выбор (1/2/3): ").strip()
        if choice in ('1', '2', '3'):
            break
        print("Ошибка: выберите 1, 2 или 3.")

    if choice == '1':
        while True:
            try:
                n = int(input("Введите количество символов n: "))
                if n <= 0:
                    print("n должно быть положительным.")
                    continue
                print("Введите последовательность символов (включая пробелы).")
                print("Пробелы считаются символами.")
                raw = input()
                if len(raw) != n:
                    print(f"Ожидалось {n} символов, введено {len(raw)}.")
                    continue
                return n, list(raw)
            except ValueError:
                print("Ошибка ввода. Повторите.")

    elif choice == '2':
        n = random.randint(5, 15)
        # Генерируем случайные символы из букв, цифр и пробелов
        chars = string.ascii_letters + string.digits + ' ' * 2
        s = [random.choice(chars) for _ in range(n)]
        print(f"\nСгенерирована случайная последовательность длиной {n}:")
        print("Символы:", s)
        return n, s

    else:  # готовые примеры
        examples = [
            (8, list("abcabcde")),   # последние вхождения: d, e? проверка: a(4), b(5), c(6), d(7), e(8) -> d, e? но там "abcabcde" c? индексы: a:0,3; b:1,4; c:2,5; d:6; e:7. Порядок последних: c(5), d(6), e(7)? на самом деле a(3),b(4),c(5),d(6),e(7) -> d,e? нет, a(3) раньше, чем c(5). Последние вхождения: a(3), b(4), c(5), d(6), e(7). Последовательность будет содержать символы: a? нет, надо смотреть итерацию: i=0 a: last=0, not last; i=1 b: last=1; i=2 c: last=2; i=3 a: last=3 -> добавим a; i=4 b: last=4 -> b; i=5 c: last=5 -> c; i=6 d: last=6 -> d; i=7 e: last=7 -> e. Итог: a b c d e. Но исходная "abcabcde" => после удаления повторений остаётся "abcde"? Последние вхождения: a(3),b(4),c(5),d(6),e(7) -> символы a,b,c,d,e. Это правильно.
            (10, list("a b c a b")),  # последние: пробел? индексы: 0:a,1:space,2:b,3:space,4:c,5:space,6:a,7:space,8:b -> последние: a(6), space(7), b(8), c(4). Порядок появления последних: c(4), a(6), space(7), b(8) -> "c a  b"? Проблема в том, что space встречается несколько раз, последний на 7. Так что последовательность: c, a, space, b.
            (6, list("112233")),      # последние: 1(1),2(3),3(5) -> "123"? но порядок последних: 1(1),2(3),3(5) -> 1,2,3.
            (7, list("abcdabc")),     # последние: a(4),b(5),c(6),d(3) -> порядок: d(3),a(4),b(5),c(6) -> d,a,b,c.
        ]
        print("\nГотовые примеры:")
        for idx, (n_val, seq) in enumerate(examples, 1):
            print(f"{idx}: n={n_val}, последовательность: {seq}")
        while True:
            try:
                num = int(input("Выберите номер примера: "))
                if 1 <= num <= len(examples):
                    return examples[num - 1]
                else:
                    print(f"Номер должен быть от 1 до {len(examples)}.")
            except ValueError:
                print("Ошибка ввода. Введите целое число.")


def main():
    n, s = get_sequence()

    # Запоминаем последний индекс для каждого символа
    last_index = {}
    for i, ch in enumerate(s):
        last_index[ch] = i

    # Собираем символы, у которых текущая позиция является последней
    result = [ch for i, ch in enumerate(s) if last_index[ch] == i]

    # Вывод результатов
    print("\nРезультаты:")
    print(f"Исходная последовательность: {s}")
    print(f"Последние индексы символов:  {last_index}")
    print(f"Последние вхождения в порядке их появления: {result}")


if __name__ == "__main__":
    main()
    input("\nНажмите Enter, чтобы завершить программу.")