"""
ГЛАВА 1
ОСНОВНЫЕ ПРИЁМЫ ПРОГРАММИРОВАНИЯ
14. Вычисления с хранением последовательностей, число членов
которых зависит от исходных данных *)
*) В некоторых языках программирования допускаются массивы с динамическими границами, и это снимает многие трудности в решении 
задач; в этом случае настоящий параграф продолжает § 9. В Паскале же, например, где такие массивы не допускаются, естественно 
использовать списки. Возможный вид этих списков указан в задачах 531-534. Для работы со списками полезны процедуры вставки 
элемента в начало списка, вставки элемента в конец списка, удаление 
элемента и т. д. (эти процедуры отдельно рассмотрены в §36). Для решения задач этого параграфа можно использовать и файлы, но это
резко увеличивает время выполнения программы и имеет смысл в том 
случае, когда все исходные данные не помещаются в памяти вычислительной машины.

539. Даны натуральное число n, целые числа a1, ..., an. Требуется получить последовательность x1, y1, x2, y2, ..., xk, yk, где x1, ..., xm - взятые в порядке следования четные члены последовательности a1, ..., an, а y1, ..., yl - нечетные члены, k = min(m, l).
"""


def main():
    print("=== ЗАДАЧА 539: Чередование чётных и нечётных членов последовательности ===\n")
    
    try:
        n = int(input("Введите количество чисел n (n >= 1): "))
        if n < 1:
            print("Ошибка: n должно быть больше 0.")
            return
            
        print(f"Введите {n} целых чисел через пробел:")
        a = list(map(int, input().split()))
        
        if len(a) != n:
            print(f"Ошибка: введено {len(a)} чисел, а ожидается {n}.")
            return

        # Разделяем исходную последовательность на чётные и нечётные числа
        evens = [x for x in a if x % 2 == 0]   # x1, ..., xm (чётные)
        odds = [x for x in a if x % 2 != 0]    # y1, ..., yl (нечётные)

        # Вычисляем k = min(m, l)
        k = min(len(evens), len(odds))

        # Строим результирующую последовательность x1, y1, x2, y2, ..., xk, yk
        result = []
        for i in range(k):
            result.append(evens[i])
            result.append(odds[i])

        # Вывод результатов
        print(f"\nИсходная последовательность: {a}")
        print("-" * 40)
        print(f"Чётные числа (x):   {evens}")
        print(f"Нечётные числа (y): {odds}")
        print(f"k = min({len(evens)}, {len(odds)}) = {k}")
        print(f"Результат (x1, y1, ...): {result}")

    except ValueError:
        print("Ошибка: введите корректные целые числа.")

if __name__ == "__main__":
    main()
    input("\nНажмите Enter, чтобы завершить программу.")