"""
ГЛАВА 1
ОСНОВНЫЕ ПРИЁМЫ ПРОГРАММИРОВАНИЯ
14. Вычисления с хранением последовательностей, число членов
которых зависит от исходных данных *)
*) В некоторых языках программирования допускаются массивы с динамическими границами, и это снимает многие трудности в решении 
задач; в этом случае настоящий параграф продолжает § 9. В Паскале же, например, где такие массивы не допускаются, естественно 
использовать списки. Возможный вид этих списков указан в задачах 531-534. Для работы со списками полезны процедуры вставки 
элемента в начало списка, вставки элемента в конец списка, удаление 
элемента и т. д. (эти процедуры отдельно рассмотрены в §36). Для решения задач этого параграфа можно использовать и файлы, но это
резко увеличивает время выполнения программы и имеет смысл в том 
случае, когда все исходные данные не помещаются в памяти вычислительной машины.

537. Даны натуральное число n, целые числа a1, ..., a3n.
Выяснить, верно ли, что для всех a2n + 1, ..., a3n имеются равные среди a1, ..., a2n.
"""


def main():
    print("=== ЗАДАЧА 537: Проверка вхождения чисел второй половины в первую ===\n")
    
    try:
        n = int(input("Введите натуральное число n: "))
        if n < 1:
            print("Ошибка: n должно быть больше 0.")
            return
            
        total_nums = 3 * n
        print(f"Введите {total_nums} целых чисел через пробел:")
        data = list(map(int, input().split()))
        
        if len(data) != total_nums:
            print(f"Ошибка: введено {len(data)} чисел, а ожидается {total_nums}.")
            return

        # Разбиваем список на две части согласно условию:
        # Первая часть: a1 ... a2n  (индексы от 0 до 2n-1)
        # Вторая часть: a2n+1 ... a3n (индексы от 2n до 3n-1)
        first_half = data[:2*n]
        second_half = data[2*n:]

        # Преобразуем первую часть в множество для быстрого поиска
        set_first_half = set(first_half)

        # Проверяем, верно ли, что для ВСЕХ элементов второй части
        # существуют равные им элементы в первой части.
        is_valid = all(num in set_first_half for num in second_half)

        print("\n--- Результат ---")
        if is_valid:
            print("Вывод: ДА, для всех чисел второй части имеются равные среди чисел первой части.")
        else:
            print("Вывод: НЕТ, не для всех чисел второй части имеются равные в первой части.")
            
    except ValueError:
        print("Ошибка: введите корректные целые числа.")

if __name__ == "__main__":
    main()
    input("\nНажмите Enter, чтобы завершить программу.")