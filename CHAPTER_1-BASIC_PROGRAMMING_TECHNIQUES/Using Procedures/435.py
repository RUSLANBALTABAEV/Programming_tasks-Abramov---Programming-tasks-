"""
ГЛАВА 1
ОСНОВНЫЕ ПРИЁМЫ ПРОГРАММИРОВАНИЯ
12. Использование процедур 

В задачах этого параграфа будем для краткости говорить просто о процедурах, подразумевая, что решающий задачи сам выберет подходящее средство программирования – подпрограмму, функцию и 
т. д. Этот выбор должен быть сделан с учётом как характера задач, так и особенностей используемого языка программирования.

435. Даны целые числа a1, …, an, b1, …, bm, k. Если в последовательности a1, ..., an нет ни одного члена со значением  k, то первый по порядку член этой последовательности, не меньший всех остальных членов, заменить на значение k. По такому же правилу преобразовать последовательность b1, ..., bm применительно к значению 10.
"""


import random


def process_sequence(seq, value):
    """
    Обрабатывает последовательность seq:
    если value отсутствует, заменяет первый максимальный элемент на value.
    Изменение происходит на месте, возвращается та же последовательность.
    """
    if not seq:
        return seq
    if value not in seq:
        max_val = max(seq)
        max_idx = seq.index(max_val)  # первый максимум
        seq[max_idx] = value
    return seq


def get_data():
    """Выбор способа ввода последовательностей a, b и числа k."""
    print("Задача 435: Замена первого максимума на k (или 10), если k (или 10) отсутствует")
    print("Выберите способ ввода:")
    print("1 — Ручной ввод")
    print("2 — Случайная генерация")
    print("3 — Готовые примеры")

    while True:
        choice = input("Ваш выбор (1/2/3): ").strip()
        if choice in ('1', '2', '3'):
            break
        print("Ошибка: выберите 1, 2 или 3.")

    if choice == '1':
        while True:
            try:
                n = int(input("Введите количество элементов последовательности a (n): "))
                m = int(input("Введите количество элементов последовательности b (m): "))
                k = int(input("Введите целое число k: "))
                print(f"Введите {n} целых чисел для a через пробел:")
                a = list(map(int, input().split()))
                if len(a) != n:
                    print("Ошибка: неверное количество чисел для a.")
                    continue
                print(f"Введите {m} целых чисел для b через пробел:")
                b = list(map(int, input().split()))
                if len(b) != m:
                    print("Ошибка: неверное количество чисел для b.")
                    continue
                return a, b, k
            except ValueError:
                print("Ошибка ввода. Повторите.")

    elif choice == '2':
        n = random.randint(5, 8)
        m = random.randint(5, 8)
        k = random.randint(-5, 10)
        a = [random.randint(-10, 10) for _ in range(n)]
        b = [random.randint(-10, 10) for _ in range(m)]
        print(f"Сгенерированы: n = {n}, m = {m}, k = {k}")
        print(f"a: {a}")
        print(f"b: {b}")
        return a, b, k

    else:  # готовые примеры
        examples = [
            ([2, 5, 3, 5, 1], [10, 7, 10, 2], 3),   # k=3 есть в a? нет, замена первого max=5 на 3; в b 10 есть -> без изменений
            ([1, 2, 3], [5, 4, 3, 2, 1], 3),          # k=3 есть в a, без изм.; b нет 10, max=5 -> замена на 10
            ([0, 0, 0], [1, 2, 3, 4], 5),             # a: k=5 нет, max=0 -> замена на 5; b: 10 нет, max=4 -> замена на 10
            ([8, 6, 9, 6], [15, 12, 15, 9], 10),      # a: k=10 нет, max=9 -> замена на 10; b: 10 нет, max=15 -> замена на 10
        ]
        print("Готовые примеры:")
        for idx, (a_arr, b_arr, kval) in enumerate(examples, 1):
            print(f"{idx}: a = {a_arr}, b = {b_arr}, k = {kval}")
        while True:
            try:
                num = int(input("Выберите номер примера: "))
                if 1 <= num <= len(examples):
                    return examples[num-1]
                else:
                    print(f"Номер должен быть от 1 до {len(examples)}.")
            except ValueError:
                print("Ошибка ввода. Введите целое число.")


def main():
    a, b, k = get_data()

    a_orig = a.copy()
    b_orig = b.copy()

    process_sequence(a, k)
    process_sequence(b, 10)

    print("\nРезультаты:")
    print(f"Исходная a: {a_orig}")
    print(f"Изменённая a (k = {k}): {a}")
    print(f"Исходная b: {b_orig}")
    print(f"Изменённая b (цель 10): {b}")


if __name__ == "__main__":
    main()
    input("\nНажмите Enter, чтобы завершить программу.")
