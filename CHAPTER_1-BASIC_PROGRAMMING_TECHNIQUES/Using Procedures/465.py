"""
ГЛАВА 1
ОСНОВНЫЕ ПРИЁМЫ ПРОГРАММИРОВАНИЯ
12. Использование процедур 

В задачах этого параграфа будем для краткости говорить просто о процедурах, подразумевая, что решающий задачи сам выберет подходящее средство программирования – подпрограмму, функцию и 
т. д. Этот выбор должен быть сделан с учётом как характера задач, так и особенностей используемого языка программирования.

465. Составить процедуру, позволяющую определить позицию самого правого вхождения заданного символа в исходную строку. Если строка не содержит символа, результатом работы процедуры должна быть -1.
"""


import random
import string


def rightmost_position(s, ch):
    """
    Процедура определяет позицию самого правого вхождения символа ch в строку s.
    Позиции нумеруются с 1 (как принято в текстовых задачах).
    Если символ не найден, возвращается -1.
    """
    # Используем rfind, который возвращает индекс с 0; преобразуем в 1‑based
    index = s.rfind(ch)
    if index == -1:
        return -1
    return index + 1


def get_data():
    """Выбор способа ввода строки и искомого символа."""
    print("Задача 465: Позиция самого правого вхождения символа")
    print("Выберите способ ввода:")
    print("1 — Ручной ввод")
    print("2 — Случайная генерация")
    print("3 — Готовые примеры")

    while True:
        choice = input("Ваш выбор (1/2/3): ").strip()
        if choice in ('1', '2', '3'):
            break
        print("Ошибка: выберите 1, 2 или 3.")

    if choice == '1':
        while True:
            s = input("Введите исходную строку: ")
            ch = input("Введите искомый символ (ровно один): ")
            if len(ch) != 1:
                print("Нужно ввести ровно один символ.")
                continue
            return s, ch

    elif choice == '2':
        # Генерируем случайную строку длиной 10..20
        length = random.randint(10, 20)
        pool = string.ascii_letters + string.digits + string.punctuation + ' '
        s = ''.join(random.choice(pool) for _ in range(length))
        # Ищем случайный символ, который может как присутствовать, так и отсутствовать
        if random.random() < 0.7:
            # Берём символ, который точно есть в строке
            ch = random.choice(s)
        else:
            # Генерируем заведомо отсутствующий (редкий символ)
            ch = chr(random.randint(0x0400, 0x04FF))  # кириллический, маловероятен
        print(f"Сгенерирована строка: '{s}'")
        print(f"Искомый символ: '{ch}'")
        return s, ch

    else:  # готовые примеры
        examples = [
            ("abracadabra", 'a'),
            ("hello world", 'z'),
            ("programming", 'm'),
            ("1234567890", '0'),
            ("Mississippi", 's')
        ]
        print("Готовые примеры:")
        for idx, (string_val, char_val) in enumerate(examples, 1):
            print(f"{idx}: строка='{string_val}', символ='{char_val}'")
        while True:
            try:
                num = int(input("Выберите номер примера (1-5): "))
                if 1 <= num <= len(examples):
                    return examples[num - 1]
                else:
                    print(f"Номер от 1 до {len(examples)}.")
            except ValueError:
                print("Введите целое число.")


def main():
    s, ch = get_data()
    pos = rightmost_position(s, ch)
    print(f"\nИсходная строка: '{s}'")
    print(f"Искомый символ: '{ch}'")
    if pos == -1:
        print("Символ не найден. Результат: -1")
    else:
        print(f"Самое правое вхождение на позиции: {pos}")


if __name__ == "__main__":
    main()
    input("\nНажмите Enter, чтобы завершить программу.")
