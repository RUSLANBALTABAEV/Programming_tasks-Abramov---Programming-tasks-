"""
ГЛАВА 1
ОСНОВНЫЕ ПРИЁМЫ ПРОГРАММИРОВАНИЯ
6. Пошаговый ввод данных и вывод результатов *)

166. Даны натуральное число n, действительные числа a1, ..., an.
Получить числа b1, …, bn, которые связаны с a1, ... an следующим
образом:

b1 = a1, bn = an, bi = (ai+1 - ai) / 3, i = 2, …, n - 1.
"""


def main():
    # Ввод натурального числа n
    n = int(input("Введите натуральное число n: "))
    
    if n <= 0:
        print("Ошибка: n должно быть натуральным числом!")
        return
    
    # Массив для хранения чисел a
    a = []
    
    print(f"Введите {n} действительных чисел:")
    for i in range(n):
        a_i = float(input(f"a[{i+1}]: "))
        a.append(a_i)
    
    # Вычисление массива b
    b = []
    
    # Обрабатываем особые случаи
    if n >= 1:
        b.append(a[0])  # b1 = a1
    
    if n >= 2:
        # Для i от 2 до n-1 (в 0-индексации это индексы 1 до n-2)
        for i in range(1, n-1):
            b_i = (a[i+1] - a[i]) / 3
            b.append(b_i)
        
        # Добавляем последний элемент
        b.append(a[-1])  # bn = an
    
    # Вывод результатов
    print("\nРезультаты:")
    for i in range(n):
        print(f"b[{i+1}] = {b[i]:.6f}")

if __name__ == "__main__":
    main()
    input("\n\nНажмите Enter, чтобы завершить программу.")
