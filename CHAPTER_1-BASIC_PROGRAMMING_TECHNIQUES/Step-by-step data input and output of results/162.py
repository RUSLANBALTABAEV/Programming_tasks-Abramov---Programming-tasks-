"""
ГЛАВА 1
ОСНОВНЫЕ ПРИЁМЫ ПРОГРАММИРОВАНИЯ
6. Пошаговый ввод данных и вывод результатов *)

162. Даны натуральные числа i, n, действительные числа a1, ..., an (i <= n). Найти среднее арифметическое всех чисел a1, ..., an, кроме ai.
"""


def main():
    # Ввод натуральных чисел n и i
    n = int(input("Введите натуральное число n: "))
    
    while True:
        i = int(input(f"Введите натуральное число i (1 <= i <= {n}): "))
        if 1 <= i <= n:
            break
        else:
            print(f"Ошибка: i должно быть в диапазоне от 1 до {n}")

    sum_all = 0.0  # сумма всех чисел
    count = 0      # количество чисел (кроме a_i)
    
    print(f"Введите {n} действительных чисел:")
    
    for j in range(1, n + 1):
        a = float(input(f"a[{j}]: "))
        
        if j != i:  # если это не i-й элемент
            sum_all += a
            count += 1
        else:
            ai = a  # сохраняем значение a_i (если нужно для отладки)

    if count > 0:
        average = sum_all / count
        print(f"\nСреднее арифметическое всех чисел, кроме a[{i}] = {ai}:")
        print(f"Сумма остальных чисел: {sum_all}")
        print(f"Количество чисел: {count}")
        print(f"Среднее арифметическое: {average:.6f}")
    else:
        print(f"Нет чисел для вычисления среднего (n = {n}, исключен единственный элемент)")

if __name__ == "__main__":
    main()
    input("\n\nНажмите Enter, чтобы завершить программу.")
