"""
ГЛАВА 1
ОСНОВНЫЕ ПРИЁМЫ ПРОГРАММИРОВАНИЯ
6. Пошаговый ввод данных и вывод результатов *)

156. Даны натуральное число n, действительные числа x1, ..., xn (n >= 3). Вычиcлить:
а) (x1+2*x2+x3)*(x2+2*x3+x4)...(xn-2+2*xn-1+xn);
б) (x1+x2+x3)*x2+(x2+x3+x4)*x4+...+(xn-2+xn-1+xn)*xn-1.
"""


def main():
    n = int(input("Введите натуральное число n (n >= 3): "))
    
    if n < 3:
        print("Ошибка: n должно быть >= 3")
        return
    
    x = []
    for i in range(n):
        xi = float(input(f"Введите x{i+1}: "))
        x.append(xi)
    
    # Вычисление части (а): произведение
    product_a = 1.0
    for i in range(n-2):
        factor = x[i] + 2 * x[i+1] + x[i+2]
        product_a *= factor
    
    # Вычисление части (б): сумма
    sum_b = 0.0
    for i in range(n-2):
        term = (x[i] + x[i+1] + x[i+2]) * x[i+1]  # Умножаем на средний элемент
        sum_b += term
    
    print(f"\nа) Результат произведения: {product_a:.6f}")
    print(f"б) Результат суммы: {sum_b:.6f}")

if __name__ == "__main__":
    main()
    input("\n\nНажмите Enter, чтобы завершить программу.")
