"""
ГЛАВА 1
ОСНОВНЫЕ ПРИЁМЫ ПРОГРАММИРОВАНИЯ
6. Пошаговый ввод данных и вывод результатов *)

160. Даны натуральное число n,
действительные числа a1, l1, a2, l2, ..., 
an, ln, * (l1, l2, ..., ln >= 0). 
Найти координаты конца ломаной линии, изображенной на рис. 16.
"""


import math

def main():
    n = int(input("Введите натуральное число n (количество звеньев ломаной): "))
    
    if n <= 0:
        print("Ошибка: n должно быть натуральным числом (n > 0)")
        return
    
    # Инициализируем координаты начала ломаной
    x = 0.0
    y = 0.0
    
    print("\nВведите данные для каждого звена ломаной:")
    print("(угол α в радианах, длина ℓ >= 0)")
    
    for i in range(1, n + 1):
        print(f"\nЗвено {i}:")
        alpha = float(input(f"  Введите угол α{i} (в радианах): "))
        length = float(input(f"  Введите длину ℓ{i} (>= 0): "))
        
        if length < 0:
            print(f"  Ошибка: длина ℓ{i} должна быть >= 0")
            return
        
        # Вычисляем проекции текущего звена на оси координат
        # и добавляем к текущим координатам
        x += length * math.cos(alpha)
        y += length * math.sin(alpha)
        
        # Выводим текущие координаты после добавления звена (опционально)
        print(f"  Текущие координаты после звена {i}: ({x:.4f}, {y:.4f})")
    
    print(f"\nКоординаты конца ломаной линии: ({x:.6f}, {y:.6f})")

if __name__ == "__main__":
    main()
    input("\n\nНажмите Enter, чтобы завершить программу.")
