"""
ГЛАВА 1
ОСНОВНЫЕ ПРИЁМЫ ПРОГРАММИРОВАНИЯ
6. Пошаговый ввод данных и вывод результатов *)

157. Даны натуральное число n, действительные числа a, b (b > a > 0). Получить последовательность действительных чисел y0,
y1, ..., yn, где yi = sqrt(xi), xi = a + ih, h = (b - a)/n.
"""


import math

def main():
    n = int(input("Введите натуральное число n: "))
    
    if n <= 0:
        print("Ошибка: n должно быть натуральным числом (n > 0)")
        return
    
    a = float(input("Введите a (a > 0): "))
    b = float(input(f"Введите b (b > {a}): "))
    
    if a <= 0:
        print("Ошибка: a должно быть больше 0")
        return
    
    if b <= a:
        print(f"Ошибка: b должно быть больше a (b > {a})")
        return
    
    # Вычисление шага h
    h = (b - a) / n
    
    print("\nПоследовательность y_i = sqrt(x_i), где x_i = a + i*h:")
    print("i\tx_i\ty_i")
    print("-" * 30)
    
    # Вычисление и вывод последовательности
    for i in range(n + 1):
        x_i = a + i * h
        y_i = math.sqrt(x_i)
        print(f"{i}\t{x_i:.4f}\t{y_i:.4f}")

if __name__ == "__main__":
    main()
    input("\n\nНажмите Enter, чтобы завершить программу.")
