"""
ГЛАВА 1
ОСНОВНЫЕ ПРИЁМЫ ПРОГРАММИРОВАНИЯ
6. Пошаговый ввод данных и вывод результатов *)

159. Даны натуральное число n, действительные числа a1, ..., an * (n >= 3). 
Получить b1, ..., bn-2, где bi = ai + 1 + ai + 2, i = 1, ..., n - 2.
"""


def main():
    n = int(input("Введите натуральное число n (n >= 3): "))
    
    if n < 3:
        print("Ошибка: n должно быть >= 3")
        return
    
    # Ввод последовательности a
    a = []
    for i in range(n):
        a_i = float(input(f"Введите a{i+1}: "))
        a.append(a_i)
    
    # Вычисление последовательности b
    b = []
    for i in range(n - 2):
        # b_i = a_{i+1} + a_{i+2}, но индексация в Python с 0
        # Поэтому b_i = a[i] + a[i+1], если считать, что i начинается с 1 в условии
        # Давайте уточним: в условии b_i = a_{i+1} + a_{i+2}, i = 1,...,n-2
        # Для i=1: b_1 = a_2 + a_3 → b[0] = a[1] + a[2]
        # Для i=2: b_2 = a_3 + a_4 → b[1] = a[2] + a[3]
        # ...
        # Для i=n-2: b_{n-2} = a_{n-1} + a_n → b[n-3] = a[n-2] + a[n-1]
        # Таким образом, b_k = a_{k+1} + a_{k+2}, где k от 0 до n-3
        b_i = a[i + 1] + a[i + 2]  # a[i+1] соответствует a_{i+2} в условии
        b.append(b_i)
    
    # Вывод результата
    print(f"\nПоследовательность b (длина {len(b)}):")
    for i in range(len(b)):
        print(f"b{i+1} = {b[i]:.6f}")

if __name__ == "__main__":
    main()
    input("\n\nНажмите Enter, чтобы завершить программу.")
