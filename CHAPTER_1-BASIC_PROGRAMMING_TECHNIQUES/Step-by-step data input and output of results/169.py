"""
ГЛАВА 1
ОСНОВНЫЕ ПРИЁМЫ ПРОГРАММИРОВАНИЯ
6. Пошаговый ввод данных и вывод результатов *)

169. Даны действительные числа x,
y1, …, y100 (y1 < y2 < … < y100, y1 < x <= y100). Найти натуральное k, при котором yk-1 < x <= yk.
"""


def main():
    print("Введите действительное число x:")
    x = float(input("x = "))
    
    print("\nВведите 100 действительных чисел в порядке возрастания:")
    print("(y1 < y2 < ... < y100, причем y1 < x <= y100)")
    
    # Вводим y1
    y_prev = float(input("y1 = "))
    
    # Проверка условия y1 < x
    if x <= y_prev:
        print("Ошибка: должно выполняться условие y1 < x")
        return
    
    k = None  # Искомое значение k
    
    # Обрабатываем y2 до y100
    for i in range(2, 101):
        y_curr = float(input(f"y{i} = "))
        
        # Проверка, что последовательность возрастает
        if y_curr <= y_prev:
            print(f"Ошибка: последовательность не возрастает (y{i-1} = {y_prev}, y{i} = {y_curr})")
            return
        
        # Проверяем условие: если x попадает в интервал (y_{i-1}, y_i]
        if k is None and x <= y_curr:
            k = i
        
        y_prev = y_curr
    
    # Если k не найдено (что невозможно по условию, но для надежности)
    if k is None:
        print("Ошибка: не найдено k, удовлетворяющее условию")
    else:
        print(f"\nРезультат: k = {k}")
        print(f"Поскольку выполнено условие: y{k-1} < {x} <= y{k}")

if __name__ == "__main__":
    main()
    input("\n\nНажмите Enter, чтобы завершить программу.")
