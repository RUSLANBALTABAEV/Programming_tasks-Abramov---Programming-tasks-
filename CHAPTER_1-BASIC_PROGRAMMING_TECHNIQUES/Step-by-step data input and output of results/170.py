"""
ГЛАВА 1
ОСНОВНЫЕ ПРИЁМЫ ПРОГРАММИРОВАНИЯ
6. Пошаговый ввод данных и вывод результатов *)

170. Даны натуральные числа n, a1, …, an (n >= 4). Числа a1, ..., an - это измеренные в сотых долях секунды результаты n спортсменов в беге на 100 м. Составить команду из четырех лучших бегунов для участия в эстафете 4 * 100, т. е. указать одну из четверок натуральных чисел i, j, k, l, для которой 1 <= i < j < k < l <= n и ai + aj + ak + al имеет наименьшее значение.
"""


def main():
    # Ввод количества спортсменов
    while True:
        n = int(input("Введите количество спортсменов n (n >= 4): "))
        if n >= 4:
            break
        print("Ошибка: количество спортсменов должно быть не менее 4!")
    
    # Ввод результатов спортсменов
    print(f"\nВведите результаты {n} спортсменов в сотых долях секунды:")
    results = []
    for i in range(n):
        while True:
            try:
                a = float(input(f"Результат спортсмена {i+1}: "))
                if a > 0:
                    results.append((a, i+1))  # (результат, индекс)
                    break
                else:
                    print("Результат должен быть положительным числом!")
            except ValueError:
                print("Пожалуйста, введите число!")
    
    # Находим четырех лучших (с наименьшими результатами)
    # Сортируем по результату
    sorted_results = sorted(results, key=lambda x: x[0])
    
    # Берем четыре наименьших результата
    best_four = sorted_results[:4]
    
    # Сортируем индексы по возрастанию
    indices = sorted([idx for _, idx in best_four])
    
    # Выводим результат
    print("\nКоманда из четырех лучших бегунов для эстафеты 4×100 м:")
    print(f"Индексы спортсменов: {indices[0]}, {indices[1]}, {indices[2]}, {indices[3]}")
    
    # Дополнительная информация для проверки
    print("\nДетали выбранных спортсменов:")
    total_time = 0.0
    for i, idx in enumerate(indices, 1):
        # Находим результат по индексу
        result = next(r for r, j in results if j == idx)
        print(f"Спортсмен {idx}: {result:.2f} сотых секунды")
        total_time += result
    
    print(f"Суммарное время команды: {total_time:.2f} сотых секунды")

if __name__ == "__main__":
    main()
    input("\n\nНажмите Enter, чтобы завершить программу.")
