"""
ГЛАВА 1
ОСНОВНЫЕ ПРИЁМЫ ПРОГРАММИРОВАНИЯ
6. Пошаговый ввод данных и вывод результатов *)

167. Пусть

x1 = y1 = 1; x2 = y2 = 2; xi = (yi - 1 - yi - 2) / i;

yi = (x^2i - 1 + xi - 2 + yi - 1) / i!, i = 3,4, ...

Получить:

а) x1 y1, x2, y2, ..., x25, y25;
б) y1/2, y2/3, ..., y25 / 26.
"""


import math

def main():
    n = 25  # Количество элементов согласно условию
    
    # Инициализация массивов
    x = [0.0] * n
    y = [0.0] * n
    
    # Начальные условия
    x[0] = 1.0  # x1
    y[0] = 1.0  # y1
    x[1] = 2.0  # x2
    y[1] = 2.0  # y2
    
    # Вычисление факториала итеративно
    factorial = [1] * (n + 1)  # factorial[i] = i!
    for i in range(1, n + 1):
        factorial[i] = factorial[i-1] * i
    
    # Вычисление последующих значений
    for i in range(3, n + 1):  # i от 3 до 25
        # xi = (y_{i-1} - y_{i-2}) / i
        x[i-1] = (y[i-2] - y[i-3]) / i
        
        # yi = (x_{i-1}^2 + x_{i-2} + y_{i-1}) / i!
        y[i-1] = (x[i-2]**2 + x[i-3] + y[i-2]) / factorial[i]
    
    # а) Вывод x1, y1, x2, y2, ..., x25, y25
    print("а) Значения x и y:")
    for i in range(n):
        print(f"x[{i+1}] = {x[i]:.10f}, y[{i+1}] = {y[i]:.10f}")
    
    # б) Вывод y1/2, y2/3, ..., y25/26
    print("\nб) Значения y_i/(i+1):")
    for i in range(n):
        value = y[i] / (i + 2)  # i+2 потому что i начинается с 0: y1/(1+1)=y1/2
        print(f"y[{i+1}]/{i+2} = {value:.10f}")

if __name__ == "__main__":
    main()
    input("\n\nНажмите Enter, чтобы завершить программу.")
