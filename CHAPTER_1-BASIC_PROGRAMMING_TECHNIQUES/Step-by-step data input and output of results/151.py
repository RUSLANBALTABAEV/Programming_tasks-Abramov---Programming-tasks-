"""
ГЛАВА 1
ОСНОВНЫЕ ПРИЁМЫ ПРОГРАММИРОВАНИЯ
6. Пошаговый ввод данных и вывод результатов *)

151. Даны натуральное число n, действительные положительные числа C1, ..., Cn. Значения C1, ..., Cn является емкостями n конденсаторов. Определить емкости систем конденсаторов, которые получаются последовательным и параллельным соединением исходных конденсаторов.
"""


def main():
    print("РАСЧЕТ ЭКВИВАЛЕНТНЫХ ЕМКОСТЕЙ СИСТЕМ КОНДЕНСАТОРОВ")
    print("=" * 70)
    
    n = int(input("Количество конденсаторов n: "))
    
    if n <= 0:
        print("Ошибка: n должно быть положительным целым числом!")
        return
    
    sum_parallel = 0.0
    sum_inverse = 0.0
    min_capacity = float('inf')
    max_capacity = 0.0
    
    print("\nВведите емкости конденсаторов (в микрофарадах, мкФ):")
    print("-" * 70)
    
    for i in range(1, n + 1):
        while True:
            try:
                C = float(input(f"C{i} (мкФ) = "))
                if C <= 0:
                    print("  Емкость должна быть положительной!")
                    continue
                break
            except ValueError:
                print("  Введите числовое значение!")
        
        # Обновление статистики
        sum_parallel += C
        sum_inverse += 1 / C
        min_capacity = min(min_capacity, C)
        max_capacity = max(max_capacity, C)
    
    # Вычисление результатов
    C_parallel = sum_parallel
    C_series = 1 / sum_inverse if sum_inverse > 0 else 0
    
    print("\n" + "=" * 70)
    print("РЕЗУЛЬТАТЫ:")
    print("=" * 70)
    
    print(f"\nСтатистика по исходным конденсаторам:")
    print(f"Количество: {n}")
    print(f"Минимальная емкость: {min_capacity:.3f} мкФ")
    print(f"Максимальная емкость: {max_capacity:.3f} мкФ")
    print(f"Средняя емкость: {sum_parallel / n:.3f} мкФ")
    
    print(f"\nЭквивалентные емкости:")
    print(f"Параллельное соединение всех конденсаторов: {C_parallel:.3f} мкФ")
    print(f"Последовательное соединение всех конденсаторов: {C_series:.3f} мкФ")
    
    print(f"\nПроверка соотношений:")
    print(f"C_посл ({C_series:.3f}) ≤ C_мин ({min_capacity:.3f}) - {'Да' if C_series <= min_capacity else 'НЕТ!'}")
    print(f"C_пар ({C_parallel:.3f}) ≥ C_макс ({max_capacity:.3f}) - {'Да' if C_parallel >= max_capacity else 'НЕТ!'}")
    print(f"C_пар ≥ C_посл - {'Да' if C_parallel >= C_series else 'НЕТ!'}")
    
    # Особые случаи
    if n == 1:
        print("\nОсобый случай (n=1):")
        print("При одном конденсаторе оба соединения дают одинаковый результат.")
    elif n == 2:
        print("\nОсобый случай (n=2):")
        # Нужно было бы запомнить значения, но мы их не сохраняли
        # В упрощенной версии просто выводим общую формулу
        print("Для двух конденсаторов C₁ и C₂:")
        print("  C_пар = C₁ + C₂")
        print("  C_посл = C₁·C₂ / (C₁ + C₂)")
    
    # Практические рекомендации
    print("\n" + "=" * 70)
    print("ПРАКТИЧЕСКИЕ РЕКОМЕНДАЦИИ:")
    print("-" * 70)
    
    print("\n1. Для увеличения общей емкости используйте параллельное соединение.")
    print("2. Для уменьшения общей емкости используйте последовательное соединение.")
    print("3. Последовательное соединение также увеличивает рабочее напряжение системы.")
    print("4. Параллельное соединение требует, чтобы конденсаторы имели одинаковое напряжение.")
    
    print(f"\nВ вашем случае:")
    print(f"- Чтобы получить {C_parallel:.2f} мкФ, соедините все конденсаторы параллельно.")
    print(f"- Чтобы получить {C_series:.2f} мкФ, соедините все конденсаторы последовательно.")

if __name__ == "__main__":
    main()
    input("\n\nНажмите Enter, чтобы завершить программу.")
