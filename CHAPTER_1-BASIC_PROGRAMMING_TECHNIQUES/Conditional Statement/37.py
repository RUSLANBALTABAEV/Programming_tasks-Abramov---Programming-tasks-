"""
ГЛАВА 1
ОСНОВНЫЕ ПРИЁМЫ ПРОГРАММИРОВАНИЯ
2. Разветвления
37. Даны действительные числа a, b, c. Удвоить эти числа, если a >= b >= c,
и заменить их абсолютными значениями, если это не так.
"""


def process_numbers(a, b, c):
    """
    Обрабатывает три числа согласно условию:
    - Удваивает числа, если a ≥ b ≥ c
    - Заменяет на абсолютные значения в противном случае
    
    Args:
        a (float): Первое число
        b (float): Второе число
        c (float): Третье число
    
    Returns:
        tuple: Кортеж из трёх обработанных чисел (a, b, c)
    """
    if a >= b >= c:
        # Удваиваем числа
        return a * 2, b * 2, c * 2
    else:
        # Заменяем абсолютными значениями
        return abs(a), abs(b), abs(c)


def main():
    """Основная функция программы."""
    print("Обработка трёх действительных чисел")
    print("-" * 50)
    
    try:
        # Ввод данных
        a = float(input("Введите число a: "))
        b = float(input("Введите число b: "))
        c = float(input("Введите число c: "))
        
        print("-" * 50)
        print(f"Исходные значения: a = {a}, b = {b}, c = {c}")
        
        # Обработка чисел
        new_a, new_b, new_c = process_numbers(a, b, c)
        
        # Вывод результата
        print("-" * 50)
        if a >= b >= c:
            print("Условие a ≥ b ≥ c выполняется ✅")
            print("Числа удвоены:")
        else:
            print("Условие a ≥ b ≥ c не выполняется ❌")
            print("Числа заменены абсолютными значениями:")
        
        print(f"a = {new_a}, b = {new_b}, c = {new_c}")
        
    except ValueError:
        print("Ошибка: необходимо вводить только числа!")

    input("\nНажмите Enter, чтобы завершить программу.")


if __name__ == "__main__":
    main()
