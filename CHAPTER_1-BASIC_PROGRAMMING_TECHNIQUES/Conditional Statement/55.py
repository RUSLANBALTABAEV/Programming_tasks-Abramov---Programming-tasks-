"""
ГЛАВА 1
ОСНОВНЫЕ ПРИЁМЫ ПРОГРАММИРОВАНИЯ
2. Разветвления
55. Даны действительные положительные числа a, b, c, d.
Выяснить, можно ли прямоугольник со сторонами a, b уместить
внутри прямоугольника со сторонами c, d так, чтобы каждая из сторон
одного прямоугольника была параллельна или перпендикулярна
каждой стороне второго прямоугольника.
"""


def can_fit_rectangle(a: float, b: float, c: float, d: float) -> tuple[bool, str]:
    """
    Проверяет, можно ли разместить прямоугольник a×b внутри прямоугольника c×d.

    Прямоугольник можно разместить в двух ориентациях:
    1. a ≤ c и b ≤ d (стороны параллельны)
    2. a ≤ d и b ≤ c (поворот на 90°)

    Returns:
        tuple: (can_fit, orientation)
               can_fit - bool, можно ли разместить
               orientation - строка с описанием ориентации
    """
    # Без поворота
    orientation1 = a <= c and b <= d
    # С поворотом на 90°
    orientation2 = a <= d and b <= c

    if orientation1 and orientation2:
        return True, "возможно в обеих ориентациях"
    elif orientation1:
        return True, "без поворота (a ≤ c, b ≤ d)"
    elif orientation2:
        return True, "с поворотом на 90° (a ≤ d, b ≤ c)"
    else:
        return False, "невозможно"


def main() -> None:
    """Основная функция программы."""
    print("Проверка возможности размещения прямоугольников")
    print("=" * 75)

    try:
        # Ввод сторон первого прямоугольника
        print("Введите стороны прямоугольника, который нужно вместить:")
        a = float(input("  a = "))
        b = float(input("  b = "))

        if a <= 0 or b <= 0:
            print("Ошибка: стороны должны быть положительными!")
            return

        print()

        # Ввод сторон второго прямоугольника
        print("Введите стороны прямоугольника, в который вмещаем:")
        c = float(input("  c = "))
        d = float(input("  d = "))

        if c <= 0 or d <= 0:
            print("Ошибка: стороны должны быть положительными!")
            return

        print("=" * 75)
        print(f"Прямоугольник 1 (вмещаемый): {a} × {b} (площадь {a * b:.2f})")
        print(f"Прямоугольник 2 (вмещающий): {c} × {d} (площадь {c * d:.2f})")
        print("-" * 75)

        # Проверка размещения
        can_fit, orientation = can_fit_rectangle(a, b, c, d)

        # Детальный анализ ориентаций
        orient1 = a <= c and b <= d
        orient2 = a <= d and b <= c

        print("Проверка условий:")
        print(f"  1) Без поворота: a ≤ c и b ≤ d → {a} ≤ {c}, {b} ≤ {d} → {orient1}")
        print(f"  2) С поворотом:  a ≤ d и b ≤ c → {a} ≤ {d}, {b} ≤ {c} → {orient2}")
        print("-" * 75)

        if can_fit:
            print(f"✓ Прямоугольник {a}×{b} можно разместить "
                  f"внутри {c}×{d} ({orientation}).")
            if orient1 and orient2:
                print("  Возможны оба варианта:")
                print(f"    • Без поворота: a ≤ c, b ≤ d")
                print(f"    • С поворотом:  a ≤ d, b ≤ c")
        else:
            print(f"✗ Невозможно разместить прямоугольник {a}×{b} "
                  f"внутри {c}×{d}.")
            print("  Ни одно из условий не выполняется.")

        print("=" * 75)

    except ValueError:
        print("Ошибка: необходимо вводить числа!")


if __name__ == "__main__":
    main()

input("\nНажмите Enter, чтобы завершить программу.")
