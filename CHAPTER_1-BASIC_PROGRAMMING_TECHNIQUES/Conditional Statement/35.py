"""
ГЛАВА 1
ОСНОВНЫЕ ПРИЁМЫ ПРОГРАММИРОВАНИЯ
2. Разветвления
35. Даны действительные числа x, y, z. Вычислить:
а) max(x + y + z, x * y * z)
б) (min((x + y + z) / 2, x * y * z)) ** 2 + 1
"""

# Ввод трёх действительных чисел
x = float(input("Введите действительное число x: "))
y = float(input("Введите действительное число y: "))
z = float(input("Введите действительное число z: "))

# Вычисления
sum_xyz = x + y + z
prod_xyz = x * y * z

# а) Вычисляем максимум
result_a = max(sum_xyz, prod_xyz)

# б) Вычисляем выражение с минимумом
result_b = (min((x + y + z) / 2, prod_xyz)) ** 2 + 1

# Вывод результатов
print(f"\nа) max(x + y + z, xyz) = {result_a}")
print(f"б) (min((x + y + z) / 2, xyz))² + 1 = {result_b}")

input("\nНажмите любую клавишу, чтобы завершить программу.")
