"""
ГЛАВА 1
ОСНОВНЫЕ ПРИЁМЫ ПРОГРАММИРОВАНИЯ
2. Разветвления
41. Даны три действительных числа. Выбрать из них те,
которые принадлежат интервалу (1, 3).
"""

# Ввод трёх действительных чисел
x = float(input("Введите действительное число x: "))
y = float(input("Введите действительное число y: "))
z = float(input("Введите действительное число z: "))

print("\nЧисла, принадлежащие интервалу (1, 3):")

# Проверка принадлежности интервалу (1, 3)
found = False  # флаг, чтобы понять, есть ли такие числа

if 1 < x < 3:
    print(f"x = {x}")
    found = True
if 1 < y < 3:
    print(f"y = {y}")
    found = True
if 1 < z < 3:
    print(f"z = {z}")
    found = True

if not found:
    print("Нет чисел, принадлежащих интервалу (1, 3).")

input("\nНажмите Enter, чтобы завершить программу.")
