"""
ГЛАВА 1
ОСНОВНЫЕ ПРИЕМЫ ПРОГРАММИРОВАНИЯ
2. Разветвления
33. Даны действительные числа x, y. Получить:
а) max(x, y)
б) min(x, y)
в) max(x, y), min(x, y)
"""

# Ввод данных
x = float(input("Введите действительное число x: "))
y = float(input("Введите действительное число y: "))

# а) Максимум
if x > y:
    maximum = x
else:
    maximum = y

print(f"\nа) Максимальное значение: {maximum}")

# б) Минимум
if x < y:
    minimum = x
else:
    minimum = y

print(f"б) Минимальное значение: {minimum}")

# в) Одновременный вывод
print(f"в) max(x, y) = {maximum}, min(x, y) = {minimum}")

input("\nНажмите любую клавишу, чтобы завершить программу.")
