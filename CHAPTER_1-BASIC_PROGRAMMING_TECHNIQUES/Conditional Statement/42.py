"""
ГЛАВА 1
ОСНОВНЫЕ ПРИЁМЫ ПРОГРАММИРОВАНИЯ
2. Разветвления
42. Даны действительные числа x, y (x != y). 
Меньшее из этих двух чисел заменить их полусуммой, 
а большее – их удвоенным произведением.
"""

# Ввод двух действительных чисел
x = float(input("Введите действительное число x: "))
y = float(input("Введите действительное число y: "))

# Проверяем, что x ≠ y
if x == y:
    print("Ошибка: числа должны быть различными!")
else:
    # Вычисляем полусумму и удвоенное произведение
    half_sum = (x + y) / 2
    double_product = 2 * x * y

    print("\nИсходные значения:")
    print(f"x = {x}, y = {y}")
    print("-" * 40)

    # Замена значений по условию
    if x < y:
        x = half_sum
        y = double_product
    else:
        y = half_sum
        x = double_product

    # Вывод результата
    print("Результаты после преобразования:")
    print(f"x = {x}, y = {y}")

input("\nНажмите Enter, чтобы завершить программу.")
