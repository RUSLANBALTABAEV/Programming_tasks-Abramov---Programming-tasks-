"""
ГЛАВА 1
ОСНОВНЫЕ ПРИЁМЫ ПРОГРАММИРОВАНИЯ
2. Разветвления
43. Даны три действительных числа. 
Возвести в квадрат те из них, значения которых неотрицательны.
"""

# Ввод трёх действительных чисел
x = float(input("Введите действительное число x: "))
y = float(input("Введите действительное число y: "))
z = float(input("Введите действительное число z: "))

print("\nИсходные значения:")
print(f"x = {x}, y = {y}, z = {z}")
print("-" * 40)

# Возведение в квадрат только неотрицательных чисел
if x >= 0:
    x = x ** 2
if y >= 0:
    y = y ** 2
if z >= 0:
    z = z ** 2

# Вывод результата
print("Результаты после обработки:")
print(f"x = {x}")
print(f"y = {y}")
print(f"z = {z}")

input("\nНажмите Enter, чтобы завершить программу.")
