"""
ГЛАВА 1
ОСНОВНЫЕ ПРИЁМЫ ПРОГРАММИРОВАНИЯ
2. Разветвления
36. Даны действительные числа a, b, c. Проверить,
выполняются ли неравенства a < b < c.
"""


def check_inequality(a, b, c):
    """
    Проверяет, выполняется ли неравенство a < b < c.
    
    Args:
        a (float): Первое число.
        b (float): Второе число.
        c (float): Третье число.
    
    Returns:
        bool: True, если a < b < c, иначе False.
    """
    return a < b < c


def main():
    """Основная функция программы."""
    print("Проверка неравенства a < b < c")
    print("-" * 40)

    try:
        # Ввод данных
        a = float(input("Введите число a: "))
        b = float(input("Введите число b: "))
        c = float(input("Введите число c: "))

        # Проверка неравенства
        result = check_inequality(a, b, c)

        # Вывод результата
        print("-" * 40)
        if result:
            print(f"Неравенство {a} < {b} < {c} выполняется ✅")
        else:
            print(f"Неравенство {a} < {b} < {c} не выполняется ❌")

    except ValueError:
        print("Ошибка: необходимо вводить только числа!")

    input("\nНажмите Enter, чтобы завершить программу.")


if __name__ == "__main__":
    main()
