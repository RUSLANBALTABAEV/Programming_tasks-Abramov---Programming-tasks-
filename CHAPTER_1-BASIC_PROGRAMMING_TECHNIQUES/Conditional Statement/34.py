"""
ГЛАВА 1
ОСНОВНЫЕ ПРИЕМЫ ПРОГРАММИРОВАНИЯ
2. Разветвления
34. Даны действительные числа x, y, z. Получить:
а) max(x, y, z);
б) min(x, y, z), max(x, y, z).
"""

# Ввод трёх действительных чисел
x = float(input("Введите действительное число x: "))
y = float(input("Введите действительное число y: "))
z = float(input("Введите действительное число z: "))

# а) Максимум из трёх чисел
maximum = x
if y > maximum:
    maximum = y
if z > maximum:
    maximum = z

print(f"\nа) Максимальное значение: {maximum}")

# б) Минимум и максимум из трёх чисел
minimum = x
if y < minimum:
    minimum = y
if z < minimum:
    minimum = z

print(f"б) Минимальное значение: {minimum}")
print(f"б) Максимальное значение: {maximum}")

input("\nНажмите любую клавишу, чтобы завершить программу.")
