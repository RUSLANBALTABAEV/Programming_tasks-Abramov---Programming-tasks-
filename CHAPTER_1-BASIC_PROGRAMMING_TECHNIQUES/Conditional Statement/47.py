"""
ГЛАВА 1
ОСНОВНЫЕ ПРИЁМЫ ПРОГРАММИРОВАНИЯ
2. Разветвления
47. Даны действительные положительные числа x, y, z.
а) Выяснить, существует ли треугольник с длинами сторон x, y, z.
б) Если треугольник существует, то ответить — является ли он остроугольным.
"""


def triangle_exists(x: float, y: float, z: float) -> bool:
    """Проверяет существование треугольника по неравенству треугольника.

    Args:
        x: Длина первой стороны.
        y: Длина второй стороны.
        z: Длина третьей стороны.

    Returns:
        True, если треугольник существует, иначе False.
    """
    return (x + y > z) and (x + z > y) and (y + z > x)


def is_acute_triangle(x: float, y: float, z: float) -> bool:
    """Проверяет, является ли треугольник остроугольным.

    Треугольник остроугольный, если квадрат каждой стороны
    меньше суммы квадратов двух других сторон.

    Args:
        x: Длина первой стороны.
        y: Длина второй стороны.
        z: Длина третьей стороны.

    Returns:
        True, если треугольник остроугольный, иначе False.
    """
    return (
        x**2 < y**2 + z**2
        and y**2 < x**2 + z**2
        and z**2 < x**2 + y**2
    )


def main() -> None:
    """Основная функция программы."""
    print("Проверка существования и типа треугольника")
    print("-" * 60)

    try:
        x = float(input("Введите длину стороны x: "))
        y = float(input("Введите длину стороны y: "))
        z = float(input("Введите длину стороны z: "))

        if x <= 0 or y <= 0 or z <= 0:
            print("Ошибка: все стороны должны быть положительными!")
            return

        print("-" * 60)
        print(f"Длины сторон: x = {x}, y = {y}, z = {z}")
        print("-" * 60)

        if triangle_exists(x, y, z):
            print("✓ Треугольник с такими сторонами существует")

            if is_acute_triangle(x, y, z):
                print("✓ Треугольник является остроугольным "
                      "(все углы меньше 90°)")
            else:
                print("✗ Треугольник не является остроугольным "
                      "(есть прямой или тупой угол)")
        else:
            print("✗ Треугольник с такими сторонами не существует")
            print("  (не выполняется неравенство треугольника)")

        print("-" * 60)

    except ValueError:
        print("Ошибка: необходимо вводить числа!")


if __name__ == "__main__":
    main()

input("\nНажмите Enter, чтобы завершить программу.")
