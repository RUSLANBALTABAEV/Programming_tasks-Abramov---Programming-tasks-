"""
ГЛАВА 1
ОСНОВНЫЕ ПРИЁМЫ ПРОГРАММИРОВАНИЯ
7. Сочетания цикла и разветвления

197.Вычислить 30Ei=1 * (ai - bi) ^ 2, где

ai = i, если i - нечетное,
ai = i / 2 в противном случае, 

bi = i^2, если i - нечетное, 
bi = i^3 в противном случае.
"""


def main():
    total_sum = 0.0
    
    for i in range(1, 31):
        # Вычисляем a_i
        if i % 2 != 0:  # нечетное
            a_i = i
        else:  # четное
            a_i = i / 2
        
        # Вычисляем b_i
        if i % 2 != 0:  # нечетное
            b_i = i ** 2
        else:  # четное
            b_i = i ** 3
        
        # Вычисляем квадрат разности и добавляем к сумме
        total_sum += (a_i - b_i) ** 2
    
    print(f"Сумма S = Σ (a_i - b_i)^2 от i=1 до 30: {total_sum}")

if __name__ == "__main__":
    main()
    input("\n\nНажмите Enter, чтобы завершить программу.")
