"""
ГЛАВА 1
ОСНОВНЫЕ ПРИЁМЫ ПРОГРАММИРОВАНИЯ
7. Сочетания цикла и разветвления

187. Даны натуральное число n, действительные числа a1, ..., an. В последовательности a1, ..., an все отрицательные члены увеличить на 0.5, а все неотрицательные заменить на 0.1.
"""


def main():
    # Ввод количества чисел
    n = int(input("Введите натуральное число n: "))
    
    if n <= 0:
        print("Ошибка: n должно быть натуральным числом!")
        return
    
    # Ввод последовательности действительных чисел
    numbers = []
    print(f"Введите {n} действительных чисел:")
    for i in range(n):
        while True:
            try:
                num = float(input(f"a[{i+1}]: "))
                numbers.append(num)
                break
            except ValueError:
                print("Пожалуйста, введите число!")
    
    # Преобразование последовательности
    transformed = []
    for num in numbers:
        if num < 0:  # отрицательные увеличиваем на 0.5
            transformed.append(num + 0.5)
        else:  # неотрицательные заменяем на 0.1
            transformed.append(0.1)
    
    # Вывод результатов
    print("\nПреобразованная последовательность:")
    for i in range(n):
        print(f"a[{i+1}] = {transformed[i]:.6f}")

if __name__ == "__main__":
    main()
    input("\n\nНажмите Enter, чтобы завершить программу.")
