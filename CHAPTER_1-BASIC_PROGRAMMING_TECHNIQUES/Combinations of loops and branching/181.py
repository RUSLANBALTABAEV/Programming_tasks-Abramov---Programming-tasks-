"""
ГЛАВА 1
ОСНОВНЫЕ ПРИЁМЫ ПРОГРАММИРОВАНИЯ
7. Сочетания цикла и разветвления

181. Даны целые числа a1, … , a50. Получить сумму тех чисел данной последовательности, которые
а) кратны 5;
б) нечетны и отрицательны;
в) удовлетворяют условию |ai| < i^2.
"""


def main():
    # Ввод 50 целых чисел
    print("Введите 50 целых чисел:")
    numbers = []
    for i in range(1, 51):
        while True:
            try:
                num = int(input(f"a[{i}]: "))
                numbers.append(num)
                break
            except ValueError:
                print("Пожалуйста, введите целое число!")
    
    # Инициализация сумм
    sum_a = 0  # сумма чисел, кратных 5
    sum_b = 0  # сумма нечетных отрицательных чисел
    sum_c = 0  # сумма чисел, удовлетворяющих условию |a_i| < i^2
    
    # Перебираем числа с их индексами (i начинается с 1)
    for i, num in enumerate(numbers, 1):
        # а) Кратные 5
        if num % 5 == 0:
            sum_a += num
        
        # б) Нечетные и отрицательные
        if num < 0 and num % 2 != 0:
            sum_b += num
        
        # в) Удовлетворяют условию |a_i| < i^2
        if abs(num) < i * i:
            sum_c += num
    
    # Вывод результатов
    print("\nРезультаты:")
    print(f"а) Сумма чисел, кратных 5: {sum_a}")
    print(f"б) Сумма нечетных отрицательных чисел: {sum_b}")
    print(f"в) Сумма чисел, удовлетворяющих условию |a_i| < i^2: {sum_c}")

if __name__ == "__main__":
    main()
    input("\n\nНажмите Enter, чтобы завершить программу.")
