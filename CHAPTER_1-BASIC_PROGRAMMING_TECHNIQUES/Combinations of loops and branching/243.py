"""
ГЛАВА 1
ОСНОВНЫЕ ПРИЁМЫ ПРОГРАММИРОВАНИЯ
7. Сочетания цикла и разветвления

243. Дано натуральное число n. Можно ли представить его в виде суммы двух квадратов натуральных чисел? Если можно, то
а) указать пару x, y таких натуральных чисел, что n = x * x + y * y;
б) указать все пары x, y таких натуральных чисел, что n = x * x + y * y, x >= y.
"""


import math

def main():
    n = int(input("Введите натуральное число n: "))
    
    # Задача: n = x^2 + y^2 (сумма квадратов)
    squares_pairs = []
    for x in range(1, int(math.sqrt(n)) + 1):
        y_squared = n - x*x
        if y_squared <= 0:
            continue
        y = int(math.sqrt(y_squared))
        if y*y == y_squared and y >= 1:
            # Для избежания дублирования (x, y) и (y, x), упорядочим пару
            if (y, x) not in squares_pairs:
                squares_pairs.append((min(x, y), max(x, y)))
    
    # Сортируем пары по первому элементу для порядка
    squares_pairs.sort()
    
    if squares_pairs:
        print(f"Число {n} можно представить в виде суммы двух квадратов.")
        # а) одна пара
        x, y = squares_pairs[0]
        print(f"а) Одна пара (x, y): x = {x}, y = {y} (проверка: {x}^2 + {y}^2 = {x*x + y*y})")
        # б) все пары с условием x ≥ y (в нашем списке пары упорядочены как (меньшее, большее))
        print("б) Все пары (x, y) с x ≥ y:")
        for x, y in squares_pairs:
            if x >= y:  # но в нашем списке x всегда ≤ y, поэтому для x ≥ y нужно поменять местами
                print(f"   ({x}, {y})")
            else:
                print(f"   ({y}, {x})")
    else:
        print(f"Число {n} нельзя представить в виде суммы двух квадратов натуральных чисел.")
    
    # Дополнительно: представление в виде n = x^2 + y (если это тоже требуется)
    # В условии пункта б) указано n = x^2 + y, но это, вероятно, опечатка, 
    # так как обычно задача о сумме квадратов более распространена.
    # Если всё же нужно n = x^2 + y, то следующий код:
    print("\nДополнительно (если требуется n = x^2 + y):")
    pairs_xy = []
    for x in range(1, int(math.sqrt(n)) + 1):
        y = n - x*x
        if y >= 1 and x >= y:
            pairs_xy.append((x, y))
    
    if pairs_xy:
        print(f"Число {n} можно представить в виде n = x^2 + y (с условием x ≥ y):")
        for x, y in pairs_xy:
            print(f"   x = {x}, y = {y} (проверка: {x}^2 + {y} = {x*x + y})")
    else:
        print(f"Число {n} нельзя представить в виде n = x^2 + y с условием x ≥ y.")

if __name__ == "__main__":
    main()
    input("\n\nНажмите Enter, чтобы завершить программу.")
