"""
ГЛАВА 1
ОСНОВНЫЕ ПРИЁМЫ ПРОГРАММИРОВАНИЯ
7. Сочетания цикла и разветвления

196.Даны целые числа a1, …, a50. Получить последовательность b1, …, b50 , которая отличается от исходной тем, что все нечетные
члены удвоены.
"""


def main():
    n = 50  # Фиксированное количество чисел согласно условию
    
    print(f"Введите {n} целых чисел:")
    a = []
    
    for i in range(1, n + 1):
        while True:
            try:
                num = int(input(f"a[{i}]: "))
                a.append(num)
                break
            except ValueError:
                print("Пожалуйста, введите целое число!")
    
    # Создание последовательности b
    b = []
    for i in range(n):
        if a[i] % 2 != 0:  # если число нечетное
            b.append(2 * a[i])
        else:  # если число четное
            b.append(a[i])
    
    # Вывод результатов
    print("\nРезультат (последовательность b):")
    for i in range(n):
        print(f"b[{i+1}] = {b[i]}")

if __name__ == "__main__":
    main()
    input("\n\nНажмите Enter, чтобы завершить программу.")
