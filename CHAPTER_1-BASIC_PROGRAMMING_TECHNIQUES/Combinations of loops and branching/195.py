"""
ГЛАВА 1
ОСНОВНЫЕ ПРИЁМЫ ПРОГРАММИРОВАНИЯ
7. Сочетания цикла и разветвления

195.Даны натуральное число n, действительные числа a, b, c1, … , cn. Верно ли *), что при 1 <= k <= n –1 всякий раз, когда ck < a, выполнено ck+1 > b?
*) В качестве ответов к этой и ряду других задач, в которых требуется определить истинность какого-либо утверждения, должны быть получены соответствующие текстовые сообщения.
"""


def main():
    # Ввод натурального числа n
    n = int(input("Введите натуральное число n: "))
    
    if n <= 0:
        print("Ошибка: n должно быть натуральным числом!")
        return
    
    # Ввод действительных чисел a и b
    a = float(input("Введите действительное число a: "))
    b = float(input("Введите действительное число b: "))
    
    # Ввод последовательности c₁, ..., cₙ
    c = []
    print(f"Введите {n} действительных чисел:")
    for i in range(n):
        while True:
            try:
                num = float(input(f"c[{i+1}]: "))
                c.append(num)
                break
            except ValueError:
                print("Пожалуйста, введите число!")
    
    # Проверка условия
    condition_holds = True  # предполагаем, что условие верно
    problematic_k = None
    
    # Проверяем для всех k от 1 до n-1 (в 0-индексации от 0 до n-2)
    for k in range(n - 1):
        if c[k] < a and c[k+1] <= b:  # если c_k < a и c_{k+1} не больше b
            condition_holds = False
            problematic_k = k + 1  # сохраняем номер k (начиная с 1)
            break
    
    # Вывод результата
    print("\nРезультат проверки:")
    if n == 1:
        print("n = 1, поэтому нет пар (c_k, c_{k+1}) для проверки.")
        print("Утверждение считается верным по умолчанию.")
    else:
        if condition_holds:
            print("Да, условие верно: для всех k = 1, ..., n-1, если c_k < a, то c_{k+1} > b.")
        else:
            print(f"Нет, условие не верно.")
            print(f"Для k = {problematic_k}: c_{problematic_k} = {c[problematic_k-1]} < {a}, но c_{problematic_k+1} = {c[problematic_k]} ≤ {b}.")

if __name__ == "__main__":
    main()
    input("\n\nНажмите Enter, чтобы завершить программу.")
