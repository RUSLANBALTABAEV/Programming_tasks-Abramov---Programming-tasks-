"""
ГЛАВА 1
ОСНОВНЫЕ ПРИЁМЫ ПРОГРАММИРОВАНИЯ
7. Сочетания цикла и разветвления

192. Даны целые числа a1, … , a45. Получить число
отрицательных членов последовательности a1, … , a35 и число нулевых членов всей последовательности a1, … , a45.
"""


def main():
    n = 45  # Фиксированное количество чисел согласно условию
    
    print(f"Введите {n} целых чисел:")
    numbers = []
    
    for i in range(1, n + 1):
        while True:
            try:
                num = int(input(f"a[{i}]: "))
                numbers.append(num)
                break
            except ValueError:
                print("Пожалуйста, введите целое число!")
    
    # Инициализация счетчиков
    negative_count = 0  # количество отрицательных в первых 35 элементах
    zero_count = 0      # количество нулей во всей последовательности
    
    # Подсчет отрицательных в первых 35 элементах
    for i in range(35):
        if numbers[i] < 0:
            negative_count += 1
    
    # Подсчет нулей во всей последовательности (45 элементов)
    for num in numbers:
        if num == 0:
            zero_count += 1
    
    # Вывод результатов
    print("\nРезультаты:")
    print(f"Число отрицательных членов в первых 35 элементах (a1..a35): {negative_count}")
    print(f"Число нулевых членов во всей последовательности (a1..a45): {zero_count}")

if __name__ == "__main__":
    main()
    input("\n\nНажмите Enter, чтобы завершить программу.")
