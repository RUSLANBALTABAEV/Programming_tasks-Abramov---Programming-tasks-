"""
ГЛАВА 1
ОСНОВНЫЕ ПРИЁМЫ ПРОГРАММИРОВАНИЯ
7. Сочетания цикла и разветвления

199.Даны натуральное число n, действительные числа r,a1, ..., an (n >= 2). Сколько среди точек (a1, an), (a2, an-1), ..., (an,a1) таких, которые принадлежат кругу радиуса r с центром в начале
координат?
"""


import math

def main():
    # Ввод натурального числа n
    while True:
        try:
            n = int(input("Введите натуральное число n (n ≥ 2): "))
            if n >= 2:
                break
            else:
                print("Ошибка: n должно быть ≥ 2!")
        except ValueError:
            print("Пожалуйста, введите целое число!")
    
    # Ввод радиуса r
    r = float(input("Введите действительное число r (радиус круга): "))
    
    # Ввод последовательности a1, ..., an
    a = []
    print(f"Введите {n} действительных чисел:")
    for i in range(1, n+1):
        while True:
            try:
                num = float(input(f"a[{i}]: "))
                a.append(num)
                break
            except ValueError:
                print("Пожалуйста, введите число!")
    
    # Подсчет точек, принадлежащих кругу
    count = 0
    for i in range(n):
        # Точка (a_i, a_{n-i-1})? По условию: (a1, an), (a2, a_{n-1}), ..., (a_n, a_1)
        # Для i от 0 до n-1: первая точка: i=0 -> (a[0], a[n-1])
        # Вторая: i=1 -> (a[1], a[n-2]) и так далее.
        x = a[i]
        y = a[n-1-i]
        # Проверяем принадлежность кругу радиуса r с центром в начале координат
        if x**2 + y**2 <= r**2:
            count += 1
    
    # Вывод результата
    print(f"\nКоличество точек, принадлежащих кругу радиуса {r} с центром в (0,0): {count}")

if __name__ == "__main__":
    main()
    input("\n\nНажмите Enter, чтобы завершить программу.")
