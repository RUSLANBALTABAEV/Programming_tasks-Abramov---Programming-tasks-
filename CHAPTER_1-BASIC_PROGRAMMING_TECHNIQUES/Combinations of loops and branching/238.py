"""
ГЛАВА 1
ОСНОВНЫЕ ПРИЁМЫ ПРОГРАММИРОВАНИЯ
7. Сочетания цикла и разветвления

238. Дано натуральное число n. Вычислить произведение
первых n сомножителей
"""


def main():
    n = int(input("Введите натуральное число n: "))
    product = 1.0
    
    # Первый сомножитель: 2/1
    numerator = 2
    denominator = 1
    
    for i in range(1, n + 1):
        product *= numerator / denominator
        
        # Обновляем числитель и знаменатель для следующего сомножителя
        if i % 2 == 1:
            # После нечётного шага (1, 3, 5, ...) увеличиваем знаменатель на 1
            denominator += 1
        else:
            # После чётного шага (2, 4, 6, ...) увеличиваем числитель на 2, знаменатель на 1
            numerator += 2
            denominator += 1
    
    print(f"Произведение первых {n} сомножителей: {product}")

if __name__ == "__main__":
    main()
    input("\n\nНажмите Enter, чтобы завершить программу.")
