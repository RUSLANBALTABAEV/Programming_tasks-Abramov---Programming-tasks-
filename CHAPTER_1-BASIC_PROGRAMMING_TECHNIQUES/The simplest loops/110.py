"""
ГЛАВА 1
ОСНОВНЫЕ ПРИЁМЫ ПРОГРАММИРОВАНИЯ
4. Простейшие циклы


110. Вычислить:
         1
--------------------
         1
1 + ----------------
         1
3 + ----------------
         1
5 + ----------------
    .
     .
      .
       101 + 1 / 103
"""


def main():
    # Создаем список нечетных чисел от 1 до 103
    odd_numbers = list(range(1, 104, 2))  # [1, 3, 5, ..., 103]
    
    # Начинаем с последнего элемента: 1/103
    result = 1 / odd_numbers[-1]
    
    # Обрабатываем остальные числа в обратном порядке (кроме последнего)
    for num in reversed(odd_numbers[:-1]):
        result = 1 / (num + result)
    
    print(f"Результат: {result}")


if __name__ == "__main__":
    main()
    input("\nНажмите Enter, чтобы завершить программу.")
