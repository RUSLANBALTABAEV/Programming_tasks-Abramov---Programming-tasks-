"""
ГЛАВА 1
ОСНОВНЫЕ ПРИЁМЫ ПРОГРАММИРОВАНИЯ
4. Простейшие циклы


107. Дано целое число m > 1. Получить наибольшее целое k, при котором 4 ^ k < m.
"""


def main():
    # Ввод целого числа m > 1
    m = int(input("Введите целое число m > 1: "))
    
    if m <= 1:
        print("Ошибка: m должно быть больше 1")
        return
    
    # Инициализация
    k = 0
    power = 1  # 4^0 = 1
    
    # Поиск максимального k, при котором 4^k < m
    while True:
        next_power = power * 4  # 4^(k+1)
        
        # Проверяем условие 4^(k+1) < m
        if next_power < m:
            k += 1
            power = next_power
        else:
            break
    
    # Вывод результата
    print(f"Наибольшее целое k, при котором 4^k < {m}, равно {k}")
    print(f"Проверка: 4^{k} = {power} < {m}")


if __name__ == "__main__":
    main()
    input("\nНажмите Enter, чтобы завершить программу.")
