"""
ГЛАВА 1
ОСНОВНЫЕ ПРИЁМЫ ПРОГРАММИРОВАНИЯ
4. Простейшие циклы

89. Алгоритм Евклида нахождения наибольшего общего
делителя (НОД) неотрицательных целых чисел основан на следующих
свойствах этой величины. Пусть m и n – одновременно не равные нулю
целые неотрицательные числа и пусть m ≥ n. Тогда, если n = 0, то НОД
(n, m) = m, а если n ≠ 0, то для чисел m, n и r, где r – остаток от деления
m на n, выполняется равенство НОД (m, n) = НОД (n, r). Например,
НОД(15, 6) = НОД(6, 3) = НОД(3, 0) = 3. Даны натуральные числа n, m.
а) Используя алгоритм Евклида, найти наибольший общий
делитель n и m.
б) Найти наименьшее общее кратное n и m.(Как здесь может
помочь алгоритм Евклида?)
"""


def gcd(a, b):
    """Алгоритм Евклида для нахождения НОД"""
    while b != 0:
        a, b = b, a % b
    return a


def lcm(a, b):
    """Нахождение НОК через НОД"""
    return a * b // gcd(a, b)


def main():
    print("Введите натуральные числа n и m:")
    n = int(input("n = "))
    m = int(input("m = "))

    if n < 1 or m < 1:
        print("Ошибка: n и m должны быть натуральными числами (≥ 1)")
        return

    print("\n" + "=" * 70)
    print(f"Числа: n = {n}, m = {m}")
    print("=" * 70)

    # а) НОД
    d = gcd(n, m)
    print("\nа) Наибольший общий делитель:")
    print(f"НОД({n}, {m}) = {d}")

    # б) НОК
    k = lcm(n, m)
    print("\nб) Наименьшее общее кратное:")
    print(f"НОК({n}, {m}) = {k}")

    print("\nПояснение:")
    print("НОК(n, m) = n · m / НОД(n, m)")
    print("\n" + "=" * 70)


if __name__ == "__main__":
    main()
    input("\nНажмите Enter, чтобы завершить программу.")
