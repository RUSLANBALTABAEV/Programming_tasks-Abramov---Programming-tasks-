"""
ГЛАВА 1
ОСНОВНЫЕ ПРИЁМЫ ПРОГРАММИРОВАНИЯ
4. Простейшие циклы


109. Дано натуральное число n. Вычислить 1 * 2 + 2 * 3 * 4 +...+ n * (n + 1) *...* 2 * n.
"""


def main():
    # Ввод натурального числа n
    n = int(input("Введите натуральное число n: "))
    
    if n <= 0:
        print("Ошибка: n должно быть натуральным числом")
        return
    
    # Инициализация общей суммы
    total_sum = 0
    
    # Внешний цикл по k от 1 до n
    for k in range(1, n + 1):
        # Вычисление произведения для текущего k: k * (k+1) * ... * (2k)
        product = 1
        for i in range(k, 2 * k + 1):
            product *= i
        
        # Добавление произведения к общей сумме
        total_sum += product
    
    # Вывод результата
    print(f"Сумма 1*2 + 2*3*4 + ... + n*(n+1)*...*2n для n={n} равна {total_sum}")
    
    # Дополнительный вывод: показать все члены суммы
    print("\nЧлены суммы:")
    for k in range(1, n + 1):
        product = 1
        for i in range(k, 2 * k + 1):
            product *= i
        print(f"Для k={k}: произведение от {k} до {2*k} = {product}")


if __name__ == "__main__":
    main()
    input("\nНажмите Enter, чтобы завершить программу.")
