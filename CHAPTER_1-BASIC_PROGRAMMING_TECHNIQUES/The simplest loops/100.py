"""
ГЛАВА 1
ОСНОВНЫЕ ПРИЁМЫ ПРОГРАММИРОВАНИЯ
4. Простейшие циклы

100. Пусть
x1 = x2 = x3 = 1;   xi = xi - 1 + xi - 3,  i = 4,5,...

Найти 
"""


def main():
    # Инициализация первых трех значений
    x_prev3 = 1  # x_{i-3}
    x_prev2 = 1  # x_{i-2}
    x_prev1 = 1  # x_{i-1}
    
    # Инициализация суммы
    total = 0.0
    
    # Инициализация степени двойки
    pow2 = 2.0  # 2^1 = 2
    
    # Вычисление суммы для i=1..100
    for i in range(1, 101):
        # Определение x_i в зависимости от i
        if i <= 3:
            x_current = 1
        else:
            x_current = x_prev1 + x_prev3
        
        # Добавление текущего члена к сумме
        total += x_current / pow2
        
        # Обновление степени двойки для следующей итерации
        pow2 *= 2
        
        # Обновление переменных для следующей итерации
        if i >= 3:
            # Сдвигаем значения: x_{i-3}, x_{i-2}, x_{i-1}
            x_prev3, x_prev2, x_prev1 = x_prev2, x_prev1, x_current
    
    # Вывод результата
    print(f"Сумма от i=1 до 100 (x_i / 2^i) = {total}")


if __name__ == "__main__":
    main()
    input("\nНажмите Enter, чтобы завершить программу.")
