"""
ГЛАВА 1
ОСНОВНЫЕ ПРИЁМЫ ПРОГРАММИРОВАНИЯ
4. Простейшие циклы

97. Пусть
x1 = y1 = 1; xi = 0.3 * xi-1;
yi = xi - 1 + yi - 1, i = 2,3,...
Дано натуральное n. Найти
"""


def main():
    # Ввод натурального числа n
    n = int(input("Введите натуральное число n: "))
    
    if n < 1:
        print("n должно быть натуральным числом")
        return
    
    # Инициализация начальных значений
    x_prev = 1  # x1
    y_prev = 1  # y1
    
    # Вычисление первого члена суммы
    total = x_prev / (1 + abs(y_prev))
    
    # Вычисление остальных членов последовательности (i=2..n)
    for i in range(2, n + 1):
        # Вычисление новых значений по формулам
        x_new = 0.3 * x_prev
        y_new = x_prev + y_prev
        
        # Добавление нового члена к сумме
        total += x_new / (1 + abs(y_new))
        
        # Обновление значений для следующей итерации
        x_prev, y_prev = x_new, y_new
    
    # Вывод результата
    print(f"Сумма от i=1 до {n} (x_i / (1 + |y_i|)) = {total}")

if __name__ == "__main__":
    main()
    input("\nНажмите Enter, чтобы завершить программу.")
