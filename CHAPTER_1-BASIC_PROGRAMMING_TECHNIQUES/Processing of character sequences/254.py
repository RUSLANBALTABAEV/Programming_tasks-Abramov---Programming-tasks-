"""
ГЛАВА 1
ОСНОВНЫЕ ПРИЁМЫ ПРОГРАММИРОВАНИЯ
7. Сочетания цикла и разветвления

254. Даны натуральное число n, символы s1, …, sn. Выяснить, имеются ли в последовательности s1, …, sn такие члены последовательности si, si+1, что si – это запятая, а si+1 – тире.
"""


def main():
    print("=== Задача 254 (строго по условию) ===")
    
    # Ввод данных
    n = int(input("Введите натуральное число n: "))
    
    print(f"Введите {n} символов подряд:")
    symbols = input()
    
    # Проверяем длину введенных символов
    if len(symbols) > n:
        symbols = symbols[:n]
    elif len(symbols) < n:
        print(f"Предупреждение: введено только {len(symbols)} символов")
        n = len(symbols)
    
    # Проверка наличия пары "запятая, затем тире"
    has_comma_dash = False
    
    # Проходим по последовательности до предпоследнего элемента
    for i in range(n - 1):
        if symbols[i] == ',' and symbols[i + 1] == '-':
            has_comma_dash = True
            break
    
    # Вывод результата
    if has_comma_dash:
        print("В последовательности есть пара символов: запятая, затем тире")
    else:
        print("В последовательности нет пары символов: запятая, затем тире")

if __name__ == "__main__":
    main()
