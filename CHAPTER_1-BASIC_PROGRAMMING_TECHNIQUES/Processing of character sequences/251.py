"""
ГЛАВА 1
ОСНОВНЫЕ ПРИЁМЫ ПРОГРАММИРОВАНИЯ
7. Сочетания цикла и разветвления

8. Обработка последовательностей символов *)
*) Если в используемом языке имеется возможность работы со
строками, то наряду с приведенными в параграфе задачами имеет
смысл рассмотреть аналогичные задачи, сформулированные в
терминах строк. В условии задачи 251 выписан дополнительный
строковый вариант, но в дальнейшем это уже не делается, так как
самостоятельная формулировка таких вариантов не составит труда для
решающего задачи. В каждом таком варианте число символов в строке
не вносится в исходные данные задачи, но предполагается, что оно не
превосходит максимально допустимой длины строки в используемом
языке программирования.
251. Даны натуральное число n, символы s1, …, sn. Подсчитать, сколько раз среди данных символов встречается буква x. (Строковый вариант: дана строка символов; подсчитать, сколько раз среди символов строки встречается буква x.)
"""


def main():
    print("=== Задача 251 (строго по условию) ===")
    
    # Вариант 1: Последовательность символов с заданным n
    print("\nВариант 1: Последовательность символов")
    n = int(input("Введите натуральное число n: "))
    
    print(f"Введите {n} символов подряд:")
    symbols = input()
    
    # Берем ровно n символов
    if len(symbols) > n:
        symbols = symbols[:n]
    
    # Ищем только букву 'x'
    count = 0
    for char in symbols:
        if char == 'x':
            count += 1
    
    print(f"Буква 'x' встречается {count} раз.")
    
    # Вариант 2: Строковый вариант
    print("\nВариант 2: Строковый вариант")
    text = input("Введите строку символов: ")
    
    count2 = 0
    for char in text:
        if char == 'x':
            count2 += 1
    
    print(f"Буква 'x' встречается {count2} раз.")

if __name__ == "__main__":
    main()
