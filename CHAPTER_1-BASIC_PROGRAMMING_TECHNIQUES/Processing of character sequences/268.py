"""
ГЛАВА 1
ОСНОВНЫЕ ПРИЁМЫ ПРОГРАММИРОВАНИЯ
7. Сочетания цикла и разветвления

268. Даны натуральное число n, символы s1, …, sn. Известно, что среди данных символов есть хотя бы один, отличный от пробела. 
Требуется преобразовать последовательность s1, …, sn следующим образом. Удалить группы пробелов, которыми начинается и которыми
заканчивается последовательность, а также заменить каждую внутреннюю группу пробелов одним пробелом. Если указанных групп нет в данной последовательности, то оставить последовательность без изменения.
"""


def transform_sequence(s):
    # Удаляем пробелы в начале и конце
    s = s.lstrip(' ')
    s = s.rstrip(' ')
    
    # Заменяем внутренние группы пробелов на один пробел
    result = []
    i = 0
    while i < len(s):
        if s[i] != ' ':
            result.append(s[i])
            i += 1
        else:
            result.append(' ')  # Добавляем один пробел
            # Пропускаем все следующие пробелы
            while i < len(s) and s[i] == ' ':
                i += 1
    
    return ''.join(result)

def main():
    n = int(input("Введите количество символов n: "))
    
    # Считываем n символов
    chars = []
    for i in range(n):
        char = input(f"Введите символ s{i+1}: ")
        if len(char) > 0:
            chars.append(char[0])
        else:
            chars.append(' ')  # Если введена пустая строка, считаем это пробелом
    
    # Преобразуем список символов в строку
    sequence = ''.join(chars)
    
    # Выполняем преобразование
    transformed = transform_sequence(sequence)
    
    # Выводим результат
    print("\nИсходная последовательность:")
    print(f"'{sequence}'")
    print("\nПреобразованная последовательность:")
    print(f"'{transformed}'")
    print(f"\nДлина: {len(transformed)}")

if __name__ == "__main__":
    main()
    input("\n\nНажмите Enter, чтобы завершить программу.")
