"""
ГЛАВА 1
ОСНОВНЫЕ ПРИЁМЫ ПРОГРАММИРОВАНИЯ
7. Сочетания цикла и разветвления

259. Даны натуральное число n, символы s1, …, sn. 
Преобразовать последовательность s1, …, sn, удалив каждый символ * и повторив каждый символ, отличный от *.
"""


def main():
    print("=== Задача 259 ===")
    print("Преобразование последовательности: удалить '*', а остальные символы удвоить")
    print()
    
    # Ввод данных
    try:
        n = int(input("Введите натуральное число n: "))
        if n < 0:
            print("Ошибка: n должно быть натуральным числом.")
            return
        
        print(f"Введите {n} символов подряд:")
        symbols = input()
        
        # Проверка длины введенной последовательности
        if len(symbols) < n:
            print(f"Предупреждение: введено только {len(symbols)} символов. Будем использовать их.")
            n = len(symbols)
        elif len(symbols) > n:
            print(f"Предупреждение: введено {len(symbols)} символов. Будем использовать первые {n}.")
            symbols = symbols[:n]
        
        # Обработка последовательности
        result = []
        for ch in symbols:
            if ch != '*':
                result.append(ch * 2)  # Повторяем символ дважды
            # Если символ '*', просто пропускаем
        
        # Преобразуем список в строку
        result_str = ''.join(result)
        
        # Вывод результатов
        print(f"\nИсходная последовательность: {symbols}")
        print(f"Длина последовательности: {n}")
        print(f"\nРезультат после преобразования: {result_str}")
        print(f"Длина результата: {len(result_str)}")
        
        # Подсчет символов
        star_count = symbols.count('*')
        non_star_count = n - star_count
        print(f"\nКоличество символов '*': {star_count}")
        print(f"Количество символов, отличных от '*': {non_star_count}")
        print(f"Ожидаемая длина результата: {non_star_count * 2} (удвоенное количество не-звездочек)")
        
    except ValueError:
        print("Ошибка: n должно быть натуральным числом.")
        return

if __name__ == "__main__":
    main()
    input("\n\nНажмите Enter, чтобы завершить программу.")
