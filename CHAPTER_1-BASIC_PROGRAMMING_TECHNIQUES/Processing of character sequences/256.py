"""
ГЛАВА 1
ОСНОВНЫЕ ПРИЁМЫ ПРОГРАММИРОВАНИЯ
7. Сочетания цикла и разветвления

256. Даны натуральное число n, символы s1, …, sn. Известно, что среди s1, …, sn есть по крайней мере одна запятая. Найти такое натуральное i, что:
а) si – первая по порядку запятая;
б) si – последняя по порядку запятая.
"""


def main():
    print("=== Задача 256 (строго по условию) ===")
    
    # Ввод данных
    n = int(input("Введите натуральное число n: "))
    
    print(f"Введите {n} символов подряд:")
    symbols = input()
    
    # Проверяем длину введенных символов
    if len(symbols) > n:
        symbols = symbols[:n]
    elif len(symbols) < n:
        print(f"Предупреждение: введено только {len(symbols)} символов")
        n = len(symbols)
    
    # Поиск первой запятой
    first = 0
    for i in range(n):
        if symbols[i] == ',':
            first = i + 1  # Переводим в 1-индексацию
            break
    
    # Поиск последней запятой
    last = 0
    for i in range(n-1, -1, -1):  # Идем с конца к началу
        if symbols[i] == ',':
            last = i + 1  # Переводим в 1-индексацию
            break
    
    # Вывод результата
    print(f"\nа) Первая запятая на позиции: {first}")
    print(f"б) Последняя запятая на позиции: {last}")

if __name__ == "__main__":
    main()
