"""
ГЛАВА 1
ОСНОВНЫЕ ПРИЁМЫ ПРОГРАММИРОВАНИЯ
7. Сочетания цикла и разветвления

255. Даны натуральное число n, символы s1, …, sn. Получить первое натуральное число i, для которого каждый из символов si и si+1 совпадает с буквой a. Если такой пары символов в последовательности s1, …, sn нет, то ответом должно быть число 0.
"""


def main():
    print("=== Задача 255 (строго по условию) ===")
    
    # Ввод данных
    n = int(input("Введите натуральное число n: "))
    
    print(f"Введите {n} символов подряд:")
    symbols = input()
    
    # Проверяем длину введенных символов
    if len(symbols) > n:
        symbols = symbols[:n]
    elif len(symbols) < n:
        print(f"Предупреждение: введено только {len(symbols)} символов")
        n = len(symbols)
    
    # Поиск первой пары "aa"
    i = 0  # Будем искать в 1-индексации
    
    # Проходим по последовательности от 1 до n-1
    # В условии: найти i такое, что s_i = 'a' и s_{i+1} = 'a'
    # Индексы в программе: 0..n-1, поэтому s_i соответствует symbols[i-1]
    for pos in range(1, n):  # pos - это i в 1-индексации
        if symbols[pos-1] == 'a' and symbols[pos] == 'a':
            i = pos
            break
    
    # Вывод результата
    print(f"\nОтвет: {i}")

if __name__ == "__main__":
    main()
