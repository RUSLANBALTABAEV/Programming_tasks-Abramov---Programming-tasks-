"""
ГЛАВА 1
ОСНОВНЫЕ ПРИЁМЫ ПРОГРАММИРОВАНИЯ
11. Вложенные циклы в матричных задачах

369. Получить |aij|i=1,...,10;j=1,...,12 - целочисленную матрицу, для которой aij=i+2*j.
"""


def main():
    # Размеры матрицы: 10 строк, 12 столбцов
    n_rows = 10
    n_cols = 12

    print("Целочисленная матрица a[i][j] = i + 2j (индексы i, j от 1):\n")
    for i in range(1, n_rows + 1):
        row = []
        for j in range(1, n_cols + 1):
            row.append(i + 2 * j)
        # Вывод строки с выравниванием
        print(" ".join(f"{val:4d}" for val in row))

if __name__ == "__main__":
    main()
    input("\nНажмите Enter, чтобы завершить программу.")
