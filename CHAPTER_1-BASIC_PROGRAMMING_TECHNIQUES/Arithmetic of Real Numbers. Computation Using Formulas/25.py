"""
ГЛАВА 1
ОСНОВНЫЕ ПРИЕМЫ ПРОГРАММИРОВАНИЯ
25. Треугольник задан координатами своих вершин. Найти:
а) периметр треугольника;
б) площадь треугольника.
"""

from math import sqrt


def main():
    """Вычисляет периметр и площадь треугольника по координатам вершин."""
    # Ввод данных
    x1 = float(input("Введите x1: "))
    y1 = float(input("Введите y1: "))
    x2 = float(input("Введите x2: "))
    y2 = float(input("Введите y2: "))
    x3 = float(input("Введите x3: "))
    y3 = float(input("Введите y3: "))

    # Длины сторон треугольника
    ab = sqrt((x2 - x1) ** 2 + (y2 - y1) ** 2)
    bc = sqrt((x3 - x2) ** 2 + (y3 - y2) ** 2)
    ac = sqrt((x3 - x1) ** 2 + (y3 - y1) ** 2)

    # Периметр
    perimeter = ab + bc + ac

    # Полупериметр
    p = perimeter / 2

    # Площадь по формуле Герона
    area = sqrt(p * (p - ab) * (p - bc) * (p - ac))

    # Вывод результатов
    print(f"\nПериметр треугольника: {perimeter:.2f}")
    print(f"Площадь треугольника: {area:.2f}")

    input("\nНажмите любую клавишу, чтобы завершить программу.")


if __name__ == "__main__":
    main()
