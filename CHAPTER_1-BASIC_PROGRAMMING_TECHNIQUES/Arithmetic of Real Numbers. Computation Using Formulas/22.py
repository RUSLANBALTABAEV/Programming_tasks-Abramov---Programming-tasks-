"""
ГЛАВА 1
ОСНОВНЫЕ ПРИЕМЫ ПРОГРАММИРОВАНИЯ
22. Найти площадь равнобочной трапеции с основаниями a и b
и углом alpha при большом основании a.
"""

from math import radians, tan


def main():
    # Ввод данных
    a = float(input("Введите большее основание a: "))
    b = float(input("Введите меньшее основание b: "))
    alpha_deg = float(input("Введите угол при большем основании (в градусах): "))

    if a <= b:
        print("Ошибка: основание a должно быть больше основания b.")
        return

    # Перевод угла в радианы
    alpha = radians(alpha_deg)

    # Вычисление высоты трапеции
    h = ((a - b) / 2) * tan(alpha)

    # Вычисление площади трапеции
    S = ((a + b) / 2) * h

    # Вывод
    print(f"Площадь равнобочной трапеции равна: {S:.2f}")

    input("Нажмите любую клавишу, чтобы завершить программу.")


if __name__ == "__main__":
    main()
