"""
ГЛАВА 1
ОСНОВНЫЕ ПРИЕМЫ ПРОГРАММИРОВАНИЯ
17. Найти площадь кольца, внутренний радиус которого равен 20, а внешний – заданному числу r (r > 20)
"""

from math import pi

# Ввод данных
inner_radius = 20.0
outer_radius = float(input("Введите внешний радиус (должен быть > 20): "))

# Проверка корректности радиусов
if outer_radius <= inner_radius:
    print("Ошибка: внешний радиус должен быть больше внутреннего (20)!")
else:
    # Вычисление площади кольца
    area = pi * (outer_radius ** 2 - inner_radius ** 2)
    # Вывод результата
    print(f"Площадь кольца: {area:.2f} квадратных единиц")

input("Нажмите любую клавишу, чтобы завершить программу.")
