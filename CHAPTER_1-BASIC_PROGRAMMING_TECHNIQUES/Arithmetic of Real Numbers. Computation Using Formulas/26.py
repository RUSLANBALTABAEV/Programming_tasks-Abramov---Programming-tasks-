"""
ГЛАВА 1
ОСНОВНЫЕ ПРИЕМЫ ПРОГРАММИРОВАНИЯ
26. Найти площадь сектора, радиус которого равен 13.7,
а дуга содержит заданное число радиан φ.
"""

def main():
    """Вычисляет площадь сектора по радиусу и углу в радианах."""

    from math import pi

    try:
        # Радиус задан по условию
        radius = 13.7
        print(f"Радиус сектора (по условию): {radius}")

        # Ввод угла в радианах
        angle = float(input("Введите угол φ (в радианах): "))

        # Проверка корректности
        if angle <= 0:
            print("Ошибка: угол должен быть положительным числом.")
            return
        if angle > 2 * pi:
            print("Предупреждение: угол больше 2π радиан (полного круга).")

        # Формула площади сектора: S = 1/2 * φ * r²
        area = 0.5 * angle * radius ** 2

        # Вывод результата
        print(f"\nПлощадь сектора: {area:.2f} квадратных единиц")

    except ValueError:
        print("Ошибка: введите числовое значение угла.")

    input("\nНажмите любую клавишу, чтобы завершить программу.")


if __name__ == "__main__":
    main()
