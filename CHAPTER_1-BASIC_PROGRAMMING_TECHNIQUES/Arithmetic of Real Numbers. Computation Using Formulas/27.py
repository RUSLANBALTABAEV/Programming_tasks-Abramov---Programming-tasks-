"""
ГЛАВА 1
ОСНОВНЫЕ ПРИЕМЫ ПРОГРАММИРОВАНИЯ
27. Даны действительные положительные числа a, b, c.
По трём сторонам с длинами a, b, c можно построить треугольник.
Найти углы треугольника.
"""

from math import acos, degrees

def main():
    """Вычисляет углы треугольника по трём сторонам."""

    try:
        # Ввод данных
        a = float(input("Введите длину стороны a: "))
        b = float(input("Введите длину стороны b: "))
        c = float(input("Введите длину стороны c: "))

        # Проверка положительности сторон
        if a <= 0 or b <= 0 or c <= 0:
            print("Ошибка: длины сторон должны быть положительными.")
            return

        # Проверка неравенства треугольника
        if not (a + b > c and a + c > b and b + c > a):
            print("Ошибка: треугольник с такими сторонами не существует.")
            return

        # Косинусы углов по закону косинусов
        cos_A = (b**2 + c**2 - a**2) / (2 * b * c)
        cos_B = (a**2 + c**2 - b**2) / (2 * a * c)
        cos_C = (a**2 + b**2 - c**2) / (2 * a * b)

        # Корректируем возможные погрешности вычислений (из-за float)
        cos_A = max(-1, min(1, cos_A))
        cos_B = max(-1, min(1, cos_B))
        cos_C = max(-1, min(1, cos_C))

        # Находим углы в радианах
        A_rad = acos(cos_A)
        B_rad = acos(cos_B)
        C_rad = acos(cos_C)

        # Переводим углы в градусы
        A_deg = degrees(A_rad)
        B_deg = degrees(B_rad)
        C_deg = degrees(C_rad)

        # Вывод результатов
        print(f"\nУгол A: {A_deg:.2f}°")
        print(f"Угол B: {B_deg:.2f}°")
        print(f"Угол C: {C_deg:.2f}°")

    except ValueError:
        print("Ошибка: введите числовые значения.")

    input("\nНажмите любую клавишу, чтобы завершить программу.")


if __name__ == "__main__":
    main()
