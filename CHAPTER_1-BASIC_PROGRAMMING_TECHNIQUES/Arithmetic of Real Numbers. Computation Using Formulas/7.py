"""
ГЛАВА 1 
ОСНОВНЫЕ ПРИЕМЫ ПРОГРАММИРОВАНИЯ
1. Арифметика действительных чисел. Вычисление по формулам.
7. Смешано v1 литров воды температуры t1 с v2 литрами воды температуры t2. 
Найти объем и температуру образовавшейся смеси.
"""

# Ввод данных
v1 = float(input("Объем v1 (л): "))
t1 = float(input("Температура t1 (°C): "))
v2 = float(input("Объем v2 (л): "))
t2 = float(input("Температура t2 (°C): "))

# Вычисления
v_total = v1 + v2
t_mix = (v1 * t1 + v2 * t2) / v_total

# Вывод результата
print(f"Объем смеси: {v_total:.2f} л")
print(f"Температура смеси: {t_mix:.2f} °C")

input("Нажмите любую клавишу, чтобы завершить консольную программу.")
