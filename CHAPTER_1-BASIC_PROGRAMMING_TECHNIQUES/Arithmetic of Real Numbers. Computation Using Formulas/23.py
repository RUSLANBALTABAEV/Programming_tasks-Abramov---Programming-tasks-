"""
ГЛАВА 1
ОСНОВНЫЕ ПРИЕМЫ ПРОГРАММИРОВАНИЯ
23. Треугольник задан длинами сторон. Найти:
а) длины высот;
б) длины медиан;
в) длины биссектрис;
г) радиусы вписанной и описанной окружностей.
"""

from math import sqrt


def main():
    # Ввод данных
    a = float(input("Введите сторону a: "))
    b = float(input("Введите сторону b: "))
    c = float(input("Введите сторону c: "))

    # Проверка существования треугольника
    if a + b <= c or a + c <= b or b + c <= a:
        print("Ошибка: такого треугольника не существует.")
        return

    # Полупериметр и площадь по формуле Герона
    s = (a + b + c) / 2
    area = sqrt(s * (s - a) * (s - b) * (s - c))

    # Высоты
    h_a = 2 * area / a
    h_b = 2 * area / b
    h_c = 2 * area / c

    # Медианы
    m_a = 0.5 * sqrt(2 * b**2 + 2 * c**2 - a**2)
    m_b = 0.5 * sqrt(2 * a**2 + 2 * c**2 - b**2)
    m_c = 0.5 * sqrt(2 * a**2 + 2 * b**2 - c**2)

    # Биссектрисы
    l_a = sqrt(b * c * ((b + c)**2 - a**2)) / (b + c)
    l_b = sqrt(a * c * ((a + c)**2 - b**2)) / (a + c)
    l_c = sqrt(a * b * ((a + b)**2 - c**2)) / (a + b)

    # Радиусы окружностей
    in_radius = area / s
    circum_radius = (a * b * c) / (4 * area)

    # Вывод результатов
    print("\nВысоты:")
    print(f"h_a = {h_a:.2f}, h_b = {h_b:.2f}, h_c = {h_c:.2f}")

    print("\nМедианы:")
    print(f"m_a = {m_a:.2f}, m_b = {m_b:.2f}, m_c = {m_c:.2f}")

    print("\nБиссектрисы:")
    print(f"l_a = {l_a:.2f}, l_b = {l_b:.2f}, l_c = {l_c:.2f}")

    print("\nРадиусы:")
    print(f"Вписанная окружность: r = {in_radius:.2f}")
    print(f"Описанная окружность: R = {circum_radius:.2f}")

    input("\nНажмите любую клавишу, чтобы завершить программу.")


if __name__ == "__main__":
    main()
