"""
ГЛАВА 1
ОСНОВНЫЕ ПРИЁМЫ ПРОГРАММИРОВАНИЯ
9. Вычисления с хранением последовательности значений

271. Даны действительные числа а1, …, а15. Получить

a = 1 / 15 * 15Ei=1 * ai, sqrt(15Ei=1 * (ai - a) * (ai - a) / 14).
"""


def main():
    # Считываем 15 действительных чисел
    numbers = []
    for i in range(15):
        while True:
            try:
                num = float(input(f"Введите действительное число a_{i+1}: "))
                numbers.append(num)
                break
            except ValueError:
                print("Ошибка: введите действительное число (например, 3.14)")

    # Вычисляем среднее арифметическое
    mean = sum(numbers) / 15

    # Вычисляем сумму квадратов отклонений от среднего
    sum_squared_diff = sum((x - mean) ** 2 for x in numbers)

    # Вычисляем стандартное отклонение (несмещенная оценка)
    std_deviation = (sum_squared_diff / 14) ** 0.5

    # Выводим результаты
    print(f"\nСреднее арифметическое: {mean:.6f}")
    print(f"Стандартное отклонение: {std_deviation:.6f}")

if __name__ == "__main__":
    main()
    input("\n\nНажмите Enter, чтобы завершить программу.")
