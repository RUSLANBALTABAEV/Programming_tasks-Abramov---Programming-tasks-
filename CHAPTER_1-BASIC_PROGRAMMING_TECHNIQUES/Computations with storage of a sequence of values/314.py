"""
ГЛАВА 1
ОСНОВНЫЕ ПРИЁМЫ ПРОГРАММИРОВАНИЯ
9. Вычисления с хранением последовательности значений

314. Даны символы s1, …, s66. Если последовательность s1,… , s66 такова, что s1 = s34, s2 = s35, …, s33 = s66, то оставить ее без изменения, иначе получить последовательность s1, s2, … , s66, s1, s2,…, s66.
"""


def main():
    print("Задача 314. Проверка совпадения первой и второй половин строки из 66 символов.")
    print("Если s1=s34, s2=s35, ..., s33=s66, строка остаётся без изменений.")
    print("Иначе строка удваивается (две копии подряд).")

    # Выбор способа ввода
    print("\nВыберите способ задания строки:")
    print("1. Ввести строку вручную (ровно 66 символов)")
    print("2. Использовать готовый пример")

    while True:
        try:
            choice = int(input("Ваш выбор (1-2): "))
            if choice in (1, 2):
                break
            else:
                print("Ошибка: введите 1 или 2.")
        except ValueError:
            print("Ошибка: введите целое число.")

    s = ""
    if choice == 1:
        while True:
            s = input("Введите строку из 66 символов: ")
            if len(s) == 66:
                break
            else:
                print(f"Ошибка: длина строки должна быть 66, а у вас {len(s)}. Попробуйте снова.")
    else:
        examples = [
            ("а" * 33 + "а" * 33, "Все символы 'а', условие выполнено"),
            ("а" * 33 + "б" * 33, "Первая половина 'а', вторая 'б' — условие не выполнено"),
            ("123456789012345678901234567890123456789012345678901234567890123456", "Цифры: первые 33 и последние 33 одинаковы? Здесь первые 33 = '123456789012345678901234567890123', последние 33 = '456789012345678901234567890123456' — не равны, условие не выполнено"),
            ("abcdefghijklmnopqrstuvwxyzABCDEFGHIJKLMNOPQRSTUVWXYZ0123456789", "Разные символы, длина 62? Нет, здесь 62, но для примера не подходит. Лучше создадим точный пример.")
        ]
        # Создадим точные примеры длиной 66
        ex1 = "A" * 33 + "A" * 33  # условие выполнено
        ex2 = "A" * 33 + "B" * 33  # не выполнено
        ex3 = "1234567890" * 6 + "123456"  # 60+6=66, но первые 33 и последние 33? Сложно.
        # Для простоты используем два примера
        examples = [
            (ex1, "33 буквы 'A', затем ещё 33 'A' — условие выполнено"),
            (ex2, "33 буквы 'A', затем 33 буквы 'B' — условие не выполнено"),
            ("abcdefghijklmnopqrstuvwxyzabcdefghijklmnopqrstuvwxyz0123456789", "Строка из 26+26+10=62, не хватает. Лучше другой.")
        ]
        # Сделаем третий пример: первые 33 = "abc...", последние 33 = "ABC..."
        import string
        first = string.ascii_lowercase[:33]  # 33 буквы a-z? но их 26, недостаточно. Заполним циклом.
        first = ''.join(chr(ord('a') + i % 26) for i in range(33))
        second = ''.join(chr(ord('A') + i % 26) for i in range(33))
        ex3 = first + second  # не равны
        examples.append((ex3, "Первые 33 строчные латинские, последние 33 заглавные — не равны"))

        print("Выберите готовый пример:")
        for i, (ex, desc) in enumerate(examples, 1):
            print(f"{i}. {desc}")
            print(f"   Строка: {ex[:20]}... (длина {len(ex)})")
        while True:
            try:
                ex_choice = int(input("Номер примера (1-{}): ".format(len(examples))))
                if 1 <= ex_choice <= len(examples):
                    s = examples[ex_choice-1][0]
                    break
                else:
                    print(f"Ошибка: введите число от 1 до {len(examples)}.")
            except ValueError:
                print("Ошибка: введите целое число.")

    print(f"\nИсходная строка (длина {len(s)}):")
    print(s)

    # Проверка условия
    if len(s) == 66 and s[:33] == s[33:]:
        print("Условие выполнено: первые 33 равны последним 33.")
        result = s
    else:
        print("Условие не выполнено (или длина не 66, но по условию должно быть 66).")
        result = s + s

    print(f"\nРезультат (длина {len(result)}):")
    print(result)

    # Посимвольный вывод (первые и последние несколько)
    print("\nПервые 10 символов результата:", result[:10])
    print("Последние 10 символов результата:", result[-10:])

if __name__ == "__main__":
    main()
    input("\nНажмите Enter, чтобы завершить программу.")
