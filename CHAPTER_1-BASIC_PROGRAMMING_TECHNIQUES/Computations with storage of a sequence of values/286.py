"""
ГЛАВА 1
ОСНОВНЫЕ ПРИЁМЫ ПРОГРАММИРОВАНИЯ
9. Вычисления с хранением последовательности значений

286. Даны целые числа a1, …, a99. Получить новую последовательность, выбросив из исходной все члены со значением max (a1, …, a99).
"""


def main():
    n = 99  # количество элементов
    
    # Ввод последовательности целых чисел
    a = []
    for i in range(n):
        while True:
            try:
                value = int(input(f"Введите целое число a_{i+1}: "))
                a.append(value)
                break
            except ValueError:
                print("Ошибка: введите целое число.")

    # Находим максимальное значение
    max_value = max(a)
    print(f"\nМаксимальное значение в последовательности: {max_value}")

    # Создаем новую последовательность без элементов, равных максимуму
    new_sequence = [x for x in a if x != max_value]

    # Вывод результатов
    print(f"\nИсходная последовательность (первые 10 и последние 10 элементов):")
    print(f"Первые 10: {a[:10]}")
    print(f"Последние 10: {a[-10:]}")

    print(f"\nНовая последовательность (без элементов равных {max_value}):")
    if new_sequence:
        print(f"Количество элементов в новой последовательности: {len(new_sequence)}")
        print(f"Первые 10 (если есть): {new_sequence[:10]}")
        print(f"Последние 10 (если есть): {new_sequence[-10:]}")
    else:
        print("Новая последовательность пуста (все элементы были максимальными).")

if __name__ == "__main__":
    main()
    input("\n\nНажмите Enter, чтобы завершить программу.")
